# Sprint: Pre-Data Improvements
**Branch:** `claude/explore-plan-next-steps-3jlhmh`  
**Goal:** Build robustness, tests, and dashboard features while waiting for data to accumulate (7d deltas arrive ~2026-06-16; full 30d picture ~2026-07-09)

---

## Board

### 🔴 Backlog

#### Trade Lifecycle (WS4 — trade tickets)

Design: ADR-014 · roadmap § WS4 · mock `planning/mocks/ws4-trade-ticket.html` · issue #263.

| # | Task | File(s) | Effort | Notes |
|---|------|---------|--------|-------|
| WS4-A | ~~**ADR-014 + approved mock + CLAUDE.md rule**~~ | `knowledge/decisions/ADR-014-*`, `planning/mocks/ws4-trade-ticket.html`, `CLAUDE.md` | S | ✅ Phase A (this PR). Design gate; docs only. |
| WS4-B | ~~**PWA trade-ticket surface**~~ | `docs/index.html`, `docs/sw.js`, `docs/releases.json`, `tests/test_pwa_trade_ticket.py` | M | ✅ **Done 2026-08-09.** Expands actionable (triggered/gapped) morning cards into the ticket: join ⋈ `picks_latest`; 4-base stop menu; two gates (ATR-from-LoD 0.8/1.0 + ATR-ext-50MA 2.5/4.0 via `deriveRiskMetrics`); overridable snapshot price recomputing gates+sizing in place; free-input risk (localStorage); earnings guardrail **reusing** `EARNINGS_IMMINENT_DAYS`/`EARNINGS_CAUTION_DAYS` (no new constant — ADR-014 §7 amended); pick-reason header; `no_quote`/no-match degrade. No backend change. Release triplet shipped. 6 Playwright tests pass (verified live via revision-symlink harness); added to `tests.yml --ignore`. **Follow-up fix (2026-08-09, PR review):** the ticket's "Price now" snapshot label was hardcoded as "10:05 read" regardless of the row's real `collected_at` — misleading on a day the self-healing dispatch window (CLAUDE.md § Automation) pushed the actual morning collect past 10:05 ET. Now derived via `freshnessLabel()`, same helper the Morning-tab header already uses. 7th Playwright test added; release `2026.08.09.1` (fix), `sw.js` v59→v60. |
| WS4-C | **Pre-close (15:30) ticket rendering** | `docs/index.html` | S | **Likely satisfied for free by WS3b (PR #327):** the trade ticket renders on any actionable card via `morningCardBody`/`ws4ToggleAffordance`, and WS3b routes those helpers through `activeSessionRows()`, so a pre-close Triggered card already expands the ticket against pre_close prices. **Verify** once the 15:30 store has live rows; close if confirmed. (Time corrected 15:50→15:30 to match the WS3b session.) |
| WS4-RT | **Revisit with real-time quotes (Alpaca)** (#287) | — | — | Parked. Replace snapshot-read price with a live feed; keep manual override as fallback. Not scheduled. |
| WS4-LOW | ~~**Low-override input for the ATR-from-LoD gate**~~ | `docs/index.html`, `tests/test_pwa_trade_ticket.py` | S | ✅ **Done 2026-08-28.** Owner reported a Reclaim card's "ATR from LoD" reading as implausibly clean (0.1) vs. the ticker's actual intraday low on their broker's chart. The gate's `low` input (`r.low`, the morning scrape's `Low` column) had no correction path — unlike "Price now," which was already editable — so a stale or wrong scraped low silently fed both the gate label and had no fix for the rest of the session. Added a second "Low so far" input (`ws4-low-${ticker}`, `ts.lowOverride`, `ws4LowForCalc()` mirroring `ws4PriceForCalc()`) wired into `ws4Recompute` and `ws4TicketHtml` — same override pattern as price. Does **not** touch the "Today low" stop-basis option in the 4-way stop menu (`ws4StopLevels`), which stays on the raw scraped value — a deliberate scope decision, see `DATA-FINVIZ-LOW` below for the underlying question this doesn't resolve. New Playwright test `test_low_edit_recomputes_atr_from_lod_label` (7th→8th ticket test). No release triplet — internal tool correctness fix, not a new user-facing feature; if the owner wants it announced, add one in a follow-up. |
| WS4-LOW-STOP | ~~**Low override flows to Today-low stop + persists**~~ | `docs/index.html`, `tests/test_pwa_trade_ticket.py` | S | ✅ **Done 2026-08-30 (PR-#374 review follow-up).** Two gaps in WS4-LOW, found in review: (1) the corrected low fed only the ATR-from-LoD gate; the `today_low` stop-basis option (`ws4StopLevels`) still read raw `r.low`, so picking "Today low" after correcting sized the position — and the real `POST /positions` stop (`ws5BuildPayload`) — off a low the user had flagged as wrong. Fixed by threading `ts` into `ws4StopLevels(r, dm, ts)` and reading `today_low` through `ws4LowForCalc` (same fact → every consumer sees it); all three call sites updated. (2) `lowOverride` was in-memory only, lost on reload. Now persisted per `(ticker, date)` in localStorage (`ws4LoadLowOverride`/`ws4SaveLowOverride`/`ws4HydrateLow`, key gated on date match so a new day's fresh scrape is never shadowed). `priceOverride` deliberately left session-only (a live "price now", not a lasting fact). ATR deliberately NOT made editable (owner decision — 14-day average, doesn't move intraday). 2 new Playwright tests (`test_low_override_flows_to_today_low_stop_and_sizing`, `test_low_override_persists_across_reload`) → 12-test file green. No release triplet (same rationale as WS4-LOW). |

#### Session surfaces (WS3b — pre-close confirmation)

Scoped 2026-08-17. Spec: `planning/ws3b-preclose-surface-spec.md` · mock `planning/mocks/ws3b-preclose-toggle.html` · issue #268. Owner decisions locked: 15:30 ET, one tab + toggle, tab label stays "Morning", delta chips in, generalize-don't-clone.

| # | Task | File(s) | Effort | Notes |
|---|------|---------|--------|-------|
| WS3b-A | ~~**Writer: generalize `collect_morning.py` → `--session`**~~ | `scripts/collect_morning.py`, `scripts/session_config.py`, `tests/**`, `README.md`, `CLAUDE.md` | M | ✅ **Done (PR #327).** `--session {morning,pre_close}` (default morning); `session_store_paths()`; `write_store`/`assert_provisional` session-parameterized; `pre_close` capture_et 15:50→**15:30** (triple-doc). Morning path byte-identical (8 pre-existing tests pass unmodified); 1 new pre_close test. |
| WS3b-B | ~~**Dispatch: gated 15:30 ET job**~~ | `worker-cron/src/{routing,index}.js`, `worker-cron/test/**`, `.github/workflows/collect_preclose_status.yml`, README, CLAUDE.md | S | ✅ **Done (PR #327).** New `preclose_status` job @ 15:30 ET (own KV key, auto-derived); thin `collect_preclose_status.yml` wrapper → `collect_morning.py --session pre_close` (Option 2 — lower risk than adding `inputs` to shared dispatch). Existing `collect_preclose` backstop + #259 gate untouched. 93 worker-cron tests pass. TODO(#268): healthchecks DMS once a secret is provisioned. |
| WS3b-C | ~~**PWA: Morning tab session toggle + delta chips**~~ | `docs/index.html`, `docs/sw.js`, `docs/releases.json` | M | ✅ **Done (PR #327).** [Morning · Pre-close] toggle (segmented-pill), defaults to freshest read (`freshestSessionView`); session-specific "into the close" copy + banner; `held/firmed up/faded since AM` chips (pre-close only, morning⋈pre_close on ticker); `gapped_through`→triggered display remap at pre-close (`sessionDisplayRow`, engine untouched); ticket/take-it/chart helpers read `activeSessionRows()`. Release triplet `2026.08.17.1` / sw.js v68→v69. |
| WS3b-D | **Ship: notes + SPRINT + WS4 follow-up** | `.session/**` | XS | 🟡 In progress — see WS4-C cross-link (unblocked once the pre_close store has live rows). |

#### Trade Lifecycle (WS5 — position engine + D1)

Design: ADR-012 · `planning/trade-lifecycle-engine.md` · roadmap § WS5 · epic #264. Owner decisions 2026-08-13: worker-native Bearer auth (not CF Access), dedicated `finviz-positions` worker, provision/deploy on the shared CF account.

| # | Task | File(s) | Effort | Notes |
|---|------|---------|--------|-------|
| WS5-1-BE | ~~**Phase 1 backend: D1 + auth + "I took it" write path**~~ | `worker-positions/**`, `.github/workflows/deploy-workers.yml`, `CLAUDE.md` | L | ✅ **Done & LIVE 2026-08-13 (#309).** D1 `finviz-positions` provisioned + `migrations/0001_init.sql` applied (`positions` spine + append-only `position_events`; `ticker_quotes` deferred to phase 2 for full-width per #297). Worker deployed `finviz-positions.salmonbaby8.workers.dev`: `POST /auth/login`, ticker-generic independent-lot `POST /positions` (long-only R>0 validation, §3a/§8a), user-scoped `GET /positions`, `/health`. HMAC Bearer auth in the single swap-seam `src/auth.js` (Access-ready). Secrets set out-of-band; 28 vitest tests; live e2e smoke passed; test rows cleaned. |
| WS5-1-PWA | ~~**Phase 1 PWA: login + real "I took it" + positions read-back**~~ | `docs/index.html`, `docs/sw.js`, `docs/releases.json`, `docs/CLAUDE.md`, `tests/test_pwa_positions.py`, `tests/test_pwa_morning.py`, `tests/test_pwa_intro.py`, `.github/workflows/tests.yml` | M | ✅ **Done 2026-08-13 (#309).** Login-gated "I took it" now creates a real position: signed out → sign-in note; signed in → inline confirm (entry/stop/qty captured from the trade ticket via `ws5BuildPayload`) → `POST /positions`. New read-only **Positions** tab (sign-in card → open-positions list; frozen, honest "management arrives with the engine"). Auth client (`posLogin`/`posApi`/token in `localStorage.fv_pos_token`), stop-basis key→enum map. `taken:` marker kept (written only after 201). Dead `__morningTookIt` removed. Release triplet `2026.08.13` / sw.js v64→v65. New `test_pwa_positions.py` (6, in `--ignore`); `test_pwa_morning.py` take-it test rewritten to the new gate; `positions` added to `VALID_TAB_IDS`. Lead authored the UX spec + reviewed/ran all Playwright (24 pass via symlink harness) + 656 non-Playwright. |
| WS5-1-PASS | ~~**Rotate `POSITIONS_AUTH_PASSPHRASE` to owner's chosen value**~~ | — (CF secret) | XS | ✅ **Done 2026-08-13.** Rotated to owner's `CF_FV_PASSKEY` via `wrangler secret put`; verified live (login 200, wrong pass 401). |
| WS5-2 | ~~**Held-tickers feed → `ticker_quotes`** (#297)~~ | `worker-positions/**`, feed job | L | 🟡 Moved to **In Progress** below — see that row for current status. |
| WS5-3a | ~~**`advance()` pure engine + tests**~~ | `worker-positions/src/advance.js`, `test/advance.test.js`, README, CLAUDE.md | L | ✅ **Done (this PR).** Pure `advance(pos, bar, cfg)` + user-transitions (`confirmExit`/`stillHolding`/`autoConfirm`/`correctExit`/`reopen`) + §6 `ENGINE_CONFIG` with per-position `effectiveConfig` merge (§14) + `normalizeBar` (recovers SMA **levels** from Finviz %-distance, derives `days_to_earnings` via `parseEarningsToDays`). Profit-floor monotonic invariant, 20→50 widen, two-close soft exit (incl. deliberate 50MA-basis case), trim ledger + catch-up, gap-down/severe-breakdown/close-below-50MA exit split, idempotency + stale guards. 44 vitest (99 total, suite green). Design §9 fully covered incl. randomized property tests. No D1/endpoint/cron (→ 3b). |
| WS5-3b-i | ~~**`advance()` wiring: D1 persistence + `/advance` + trigger**~~ | `worker-positions/src/{sweep,index,auth}.js`, `test/helpers/d1.js`, `test/sweep.test.js`, `scripts/collect_held.py`, `tests/test_collect_held.py`, `.github/workflows/{tests,deploy-workers}.yml`, README, CLAUDE.md | M | ✅ **Done (this PR).** `sweep()` = catch-up fold over every `ticker_quotes` bar with `trade_date > max(last_advanced_date, entry_date)`; persists spine + append-only events in ONE `db.batch` under a compare-and-set on `last_advanced_date` (guarded event INSERTs first, CAS UPDATE last — order is load-bearing). Dual-auth `POST /advance` (service token → counts only; owner bearer → per-position `results`). Triggered from `collect_held.py` right after a successful `/ingest/quotes` — **no new cron, no new secret** (avoids the CF 5-trigger limit behind #252). Two new wiring rules, lead-decided, NOT in the design doc, both test-pinned: never advance on the entry-day bar (pre-purchase low → false `stop_hit`); persist only when `last_advanced_date` moves (else a trailing stale bar re-appends its `note` every sweep — bug caught in lead review, regression test added). Test harness moved off the hand-rolled SQL mock onto real SQLite (`node:sqlite`, zero deps) running the actual migrations; added the `worker-positions-test` PR job that never existed. 122 vitest + 670 pytest green. |
| WS5-3b-ii | ~~**Owner exit-transition routes + `autoConfirm` in the sweep**~~ | `worker-positions/src/{index,sweep,transitions}.js`, `test/{transitions,sweep}.test.js`, README, CLAUDE.md | M | ✅ **Done (this PR).** New `src/transitions.js`: owner-bearer `POST /positions/<trade_id>/{confirm-exit,still-holding,correct-exit,reopen}` over the pure fns in `advance.js` (anchored regex in `index.js`, never shadows the `/positions` collection routes). State preconditions → 409, editable+validated fill (`>0`, defaults to `expected_exit_price`), tenant-scoped load → 404, double-submit safe via `persistTransition` CAS on **`state`** (mirror of `persistAdvance`'s CAS on `last_advanced_date`; writes exactly the user-owned columns the sweep's UPDATE refuses to). `autoConfirm()` folded into `sweep()`: closes `closing` positions past `EXIT_AUTOCONFIRM_SESSIONS` at the signal-frozen `expected_exit_price` (`confirmation_status='auto'`), session clock = **global** `DISTINCT trade_date` calendar strictly after `exit_signal_date` (`sessionsSince`); new `auto_confirmed` count, `dry_run` writes nothing. 155 vitest (was 122; +33). PWA client that calls these routes is phase-4. |
| WS5-3b-OWNER | **Owner ratification: 2 wiring rules + service-token widening + auto-confirm calendar** | — (decision only) | XS | 🔴 Open, none blocking. (1) Never advance on the entry-day bar — cost is a same-day collapse through the stop isn't seen until the next bar. (2) Stale bars report via the sweep's `stale` counter + CI log rather than the `position_events` ledger. (3) `POSITIONS_INGEST_TOKEN` may now trigger `/advance`, widening its documented blast radius; alternative is a third `POSITIONS_ENGINE_TOKEN` (one `wrangler secret put` + one GH secret — the seam already exists in `src/auth.js`). (4) **NEW (3b-ii):** `autoConfirm`'s "sessions in closing" clock uses the **global** trading-session calendar (union of all held tickers' `trade_date`s), not the position's own ticker — robust to a one-symbol feed gap. Reasonable reading of design §7's "natural session calendar"; flag if owner wants per-ticker instead. |
| WS5-4a | ~~**In-app "needs your confirmation" strip**~~ | `docs/index.html` | M | ✅ **Done (PR #341, merged).** `closing` positions hoist into a collapsed "Needs your confirmation" strip (mock `ws5-needs-confirmation-surface.html` A/B/C); expand → editable Confirm-fill (`confirm-exit`, sends `exit_price` only if edited) / Still-holding (`still-holding`) + honest countdown (`auto_confirm_sessions − sessions_in_closing`, omitted not-NaN when missing). `closing` no longer renders as a card; removed dead `posClosingHeroHtml`/placeholder. Fixed a real runtime bug: inline `oninput` can't see the IIFE-scoped `state` → `window.posSetConfirmFill`. Release `2026.08.19.2` / sw v72→v73. 8 Playwright tests. Backend (routes + `sessions_in_closing`/`auto_confirm_sessions`) was pre-built: PR #340 (`8d9b4bf` review fix — `effectiveConfig(p)` not the bare global). |
| WS5-4b | ~~**VAPID push notifications (v1: Tier-1 data-less)**~~ | `worker-positions/**`, `docs/{index.html,sw.js,releases.json,CLAUDE.md}`, `tests/test_pwa_push.py` | L | ✅ **Done (PR #346 backend merged+deployed+verified; PR B PWA this session).** v1 = **Tier-1 only, data-less** (RFC 8292 VAPID auth, no RFC 8291 payload) — the two-tier salience split needs payload encryption to differentiate on the client, so Tier-2/earnings/ticker-named copy are a tracked fast-follow (**#348** / WS5-4b-PAYLOAD). **Backend (#346):** `0005_push_subscriptions.sql` (applied to live D1 out-of-band, verified) + owner-bearer `/push/subscribe`·`/push/unsubscribe` + `src/push.js` (VAPID JWT signer + `sendPush` ported verbatim from sibling `distil` worker — `web-push` npm can't run on workerd, confirmed; WebCrypto it is) + `dispatchExitPushes` (never-throws, `push_sent`-event idempotency keyed `(trade_id,trade_date)`, 410/404 self-prune, marker on success only) wired **best-effort/post-commit** into `sweep()` (Tier-1 intent at the `closing` edge, `applied && !dry_run`). 274 vitest. VAPID keypair lead-generated + round-trip verified; `VAPID_PRIVATE_KEY` live secret (owner advance sign-off), public key + subject in `wrangler.toml [vars]`. **PWA (PR B):** quiet set-once footer affordance (`posRenderAlerts`/`window.posEnableAlerts`/`posDisableAlerts`) on Positions, iOS install-to-home-screen guidance; `sw.js` `push`/`notificationclick` (data-less generic notif → deep-link Positions via `postMessage`/`#positions`); release `2026.08.20.3`/sw v75→v76; `test_pwa_push.py` (3, in `tests.yml --ignore`). Backend-first deploy order honored (route probe 401 live before PWA merge). e2e still gated on a live 17:30 held bar → real `closing` transition (same WS5 data gate). |
| WS5-4b-PAYLOAD | **RFC 8291 payload + Tier-2 reminders + earnings push** (#348) | `worker-positions/src/push.js`, `worker-positions/src/sweep.js`, `docs/sw.js` | M | 🟡 **Core done (PR #353):** RFC 8291 `aes128gcm` payload encryption + ticker-named Tier-1 exit push + SW `event.data.json()` render (data-less fallback kept). 281 vitest incl. self-round-trip decrypt gate. **Remainder still backlog (held for owner cadence sign-off):** silent decaying Tier-2 reminders (extend the `push_sent` marker with `tier`+cadence) + earnings-approach push — split off because Tier-2 carries alert-fatigue risk and needs a cadence-curve decision. |
| WS5-8-PUSH | **Push the 15:40 pre-close act-now band (WS5-8 → push)** (#349) | `worker-positions/src/preclose.js`, `worker-positions/src/push.js` | S | ✅ **Done (PR #354, stacked on #353).** `dispatchPreClosePushes` fires ticker-named "act now before the close" pushes off the 15:40 advisory, `act` severity ONLY, distinct `finviz-preclose` tag + `preclose_push_sent` marker, **advisory-only** (disjointness grep-proven — no `ingestQuotes`/`persistAdvance`/`last_advanced_date`), best-effort. Reuses PR #353's payload sender. 291 vitest. e2e gated on a live 15:40 `act` signal on a subscribed device. |
| WS5-5 | ~~**Positions tab: recently-closed grace + Closed section**~~ (#332) | `docs/index.html` | S | ✅ **Done (PR #342, this session).** Two-tier per owner design (2026-08-19): a `closed` position stays in the live list (read-only `posClosedCardHtml`, "closed" badge, realized $/R, amber "auto" cue) under a "Recently closed" divider for `POS_GRACE_SESSIONS`=2 sessions (via server `sessions_since_close`), then only in a lazy-loaded collapsible **Closed** section (`POS_CLOSED_HISTORY_SESSIONS`=60, `?closed_within_sessions=60`, fetched on first expand, dupe-excluded, `closedData` invalidated on confirm-exit). Session-based (weekend-safe), same clock as WS5-4a's countdown. Backend was PR #340 (`sessions_since_close` + `closed_within_sessions`). Release `2026.08.20.1` / sw v73→v74. 5 Playwright tests. Grace closes render grouped under a divider (not intermixed) — cleaner than literal in-list intermixing; flagged to owner, non-blocking. |
| WS5-5b | **Auto-unconfirmed-correctable strip items** | `docs/index.html` | S | 🔴 Backlog (deferred from WS5-5, non-blocking). The mock's 2nd confirmation-strip population: an *auto*-closed-unconfirmed position stays correctable in the strip for `AUTO_CLOSE_STRIP_SESSIONS`=3 sessions with an editable `correct-exit` action (route already exists, `transitions.js`, requires `state='closed'`). WS5-5 now brings `closed` rows into the fetch (within 2), so extending to 3 + a correct-exit editor in `posStripItemHtml` is the remaining work. Today an auto-closed position renders as a plain read-only closed card (with "auto" cue) — visible, just not correctable inline (correction still possible via `correct-exit` on any closed row once a UI exists). |
| WS5-WATCHADD-FIX | **Fix latent `state`-in-inline-handler bug at watchAdd retry** | `docs/index.html` | XS | 🔴 Backlog (found during WS5-4a review, non-blocking). The `watchAdd` retry button uses a bare inline `onclick`/handler referencing `state.*` directly — same IIFE-scope class as the bug fixed in PR #341 (inline HTML handlers run in global scope, can't see the IIFE-closed `state`). It throws `ReferenceError` if that error path is ever hit. Fix: route through a `window.*` closure handler like every other `window.pos*`/`window.watch*` fn. Low-frequency error path, so it hasn't bitten in practice. |
| WS5-6 | ~~**PWA: switch Positions tab off the unfiltered fetch**~~ | `docs/index.html` | S | ✅ **Done.** Backend (PR #333, merged + Cloudflare deploy independently confirmed — worker `modified_on` ~22s after the merge commit) added `GET /positions?state=open,managing,closing` (parameterized `IN` clause). `posLoadPositions()` now calls that instead of an unfiltered fetch; `POS_VISIBLE_STATES` client-side check kept as defense-in-depth, no longer load-bearing. No visible UI/behavior change (same cards render) — release triplet judged N/A, not a user-facing change. `docs/CLAUDE.md` + README `POS_VISIBLE_STATES` rows updated to describe the dual role (filter value + display set). |
| WS5-7 | ~~**Positions managing-card overhaul (HIGH PRIORITY)** (#337)~~ | `docs/index.html`, `docs/sw.js`, `docs/releases.json`, `docs/CLAUDE.md`, `worker-positions/src/{positions,transitions,index}.js`, `worker-positions/test/{index,sweep}.test.js`, `tests/test_pwa_positions.py`, `planning/ws5-7-positions-managing-card.md`, `planning/mocks/ws5-7-positions-card.html` | L | ✅ **Done (PR #338, branch `claude/pr336-cold-start-ws5-7-6sihe8`).** Shipped per `planning/ws5-7-positions-managing-card.md`. **Ack storage changed from the spec's original plan (owner call):** NOT a new `stop_ack_value` column + `0004_stop_ack.sql` migration — instead an append-only `stop_ack` `position_events` row (no migration; `event_type` has no CHECK constraint), with `GET /positions` surfacing the derived `stop_ack_value` (computed from the full per-trade event history). `POST /positions/:id/ack-stop` is event-only, disjoint from both the sweep's and transitions' write paths by construction. Backend: last-bar LEFT JOIN + bounded inline `events` + `stop_ack_value` (229 vitest). PWA: `posDerive` + state heroes + stop-moved banner/ack + Details/formulas/OHLCV/activity + trim/caution overlays; release `2026.08.19`, sw v70→v71; 8 Playwright hero-state tests (verified via chromium-symlink harness). Dollar totals show cents only when non-zero (mock-faithful). **Earnings overlay deliberately deferred → #335.** Fixes what the 2026-08-18 live-data riff exposed on the everyday `managing` card (all presentation, no engine data bug): risk math `entry−current_stop` goes $0/negative once the stop trails to/above entry (EOG showed −$3.55 open risk = the locked-in gain sign-flipped); stop-basis label lies (renders `current_stop` with the *initial* basis — OUST "$45.13 (Manual)" is really the 20MA breakeven ratchet); **user never told the stop moved → can't update their brokerage** (table-stakes for a trailing-stop engine); no engine actions/current-bar/activity visible; stale "management arrives with the engine" banner. Reframe around **last price** (4 numbers: 1R, open risk floored ≥0, locked-in, unrealized P&L) with a **state-driven hero** (risk-free 🔒 chip when stop≥entry; P&L+open-risk otherwise) + Details ▾. Small shared backend add: `GET /positions` LEFT JOINs latest `ticker_quotes` for `last_close` (§5). Stop-moved banner + **cross-device server-side ack** (owner trades phone+laptop → `stop_ack_value` on `positions`, migration `0004_stop_ack.sql` + `POST /positions/:id/ack-stop`; sweep must never write it, mirror the persist-disjointness rule) + a **pending-lock** tag (the "locked +$X" is only true once the owner actually raises their broker stop). State enum incl. overlays **trim** (`remaining_qty<initial_qty`, show "N of M sh") and **caution** (`caution_flag≥1`, "1 of 2 closes below 20MA") — both already in `advance()` output, currently hidden. Details rows show per-share + optional inline formulas + full OHLCV. **Independent of WS5-4** (that's the `closing` action strip + push; this is the everyday card) and **needs no VAPID** → ships first. Build gated on owner mock approval (`planning/mocks/ws5-7-positions-card.html`, 6 hero states w/ real EOG/OUST/NVT values). |
| WS5-8 | **Pre-close read so owner can act before the bell (15:40 ET)** (#343) | `worker-positions/**`, `scripts/collect_held.py`, `.github/workflows/`, `worker-cron/src/routing.js`, `docs/index.html` | M | 🟢 **BUILT — PR #345 open (backend + feed + cron + PWA, one branch). Live-D1 `0004` applied + verified. Awaiting merge.** Owner chose **in-app first** (push half deferred to WS5-4b), so v1 delivers act-before-bell value with no VAPID/secrets. **Timing = 15:40 ET** (owner-checked: threads between the existing 15:30 `preclose_status` picks job and 15:50 `collect_preclose` — avoids a simultaneous 2nd Finviz scrape, near-final print, ~20min runway). **Architecture (disjoint-by-construction):** new 15:40 `worker-cron` `held_preclose` job (NOT a new CF trigger) → `collect_held.py --advisory` → service-token `POST /positions/preclose-advisory`. That endpoint runs the **pure `advance()`** per open/managing held position against the 15:40 bar and upserts ONLY the result into a new `preclose_advisory` D1 table (`0004`) — writes NOTHING to `positions`/`ticker_quotes`, never stamps `last_advanced_date` (the idempotency-collision the whole design avoids; disjointness vitest #5 pins it). **Lead review caught a real bug:** the compute bypassed the sweep's entry-day window guard → a position entered today would fire a false `stop_hit` off its pre-purchase low that the 17:30 sweep never confirms → now mirrors `barWindowStart` (test #9). PWA reads owner-bearer `GET /positions/preclose` and renders an amber read-only band (act-now vs heads-up) + calm-day **read receipt** (owner asked for both). PR-1: 260 vitest + 14 pytest + 99 worker-cron. **Owner sign-off pending on applying `0004` to live D1** (out-of-band, wrangler doesn't run migrations). Mock `planning/mocks/ws5-8-preclose-read.html`. Reclaim tagging → WS5-8-RECLAIM (#344). |
| WS5-8-RECLAIM | **Tag reclaim on held / recently-closed positions (re-entry candidate)** (#344) | `worker-positions/**`, `docs/index.html` | M | 🔴 Backlog — owner idea (2026-08-20), fast-follow to WS5-8 (#343). A held or recently stopped-out position that reclaims the 20/50MA = shakeout-and-reclaim re-entry signal. Reuses the existing `reclaim` engine state (`pick_status.py`); WS5-8 v1 leaves a `category` slot in the advisory model for it. Design questions to resolve (in #344): reclaim reference (old stop vs 20/50MA), how many sessions post-close stays actionable, whether closed tickers rejoin the held scrape. The closed/re-entry case is the higher-value one. |
| WS5-BREAKEVEN-RATCHET | ~~**Breakeven ratchet: close-based vs intraday-high (owner taste call)** (#335)~~ | `worker-positions/src/advance.js`, `worker-positions/test/advance.test.js`, `docs/index.html`, `docs/{releases.json,sw.js,CLAUDE.md}`, `tests/test_pwa_positions.py` | S | ✅ **Done (this PR, branch `claude/pr336-mock-approval-plan-xcwxpy`).** Owner chose **intraday high** (2026-08-19). Built as a flippable `BREAKEVEN_TRIGGER: 'high'\|'close'` knob in `ENGINE_CONFIG` (default `'high'`) rather than hardcoding — a config/`meta.config` flip restores close-based, no logic redeploy. Also shipped both bundled items: the **negative-days earnings guard** (`advance.js` `earnings_warning` now requires `days_to_earnings >= 0` — `parseEarningsToDays` leaves a past quarter negative up to 180d, re-firing every session) and the **earnings-approaching overlay** on the positions card (`posOverlaysHtml` third block, amber ≤10 sessions / red ≤3, flag-only copy, own client-side `>= 0` guard so it's independent of the engine fix — no deploy-ordering hazard). 234/234 vitest, 5/5 release guard, 2/2 new Playwright overlay tests (symlink harness). Release triplet `2026.08.19.1` / sw v71→v72. `persistAdvance` already writes `days_to_earnings` back (sweep.js) so the overlay reflects live state. **Owner-flagged, non-blocking:** high-based is validated on the single NVT example only — not measured across trade history; the knob keeps it reversible. **Original context retained below:** `advance.js:213` ratcheted `profit_floor` to entry only when the daily **close** ≥ +1R (`BREAKEVEN_R`), never on an intraday high that tags +1R and fades. **Live example — NVT** (D1 `202c81d1…`, entry 172.35 / R 4.91 / +1R trigger 177.26): 2026-08-17 high 177.38 tagged +1R but close 177.21 fell a nickel short → floor stayed at initial 167.44 → 2026-08-18 gapped down, stopped out at 167.44 for ~−1R on a trade that was +1R in hand the prior day. In-spec (owner "+1R exactly" 2026-08-10) but the close-vs-high edge is a real taste call; high-based/touched-+1R-latch is a small change (`rMultiple(next, bar.high)` or a `BREAKEVEN_TRIGGER` knob) + one `advance.test.js` fixture. Full writeup + worked table: **issue #335** (points back here). Surface before WS5-4 makes trailing stops user-visible. **Also bundled here (WS5-7 deferral):** the **earnings-approaching overlay** — deliberately NOT built in WS5-7 because the `earnings_warning`/`days_to_earnings` engine signal fires on *negative* days-to-earnings (past dates). Fix the negative-days guard in `advance.js` first, then the PWA overlay is a small add (`docs/index.html` `posOverlaysHtml`). See `planning/ws5-7-positions-managing-card.md` §2b. |
| WS5-8a | ~~**Manual "any ticker" position entry (§8a, #264)**~~ | `docs/index.html`, `docs/sw.js`, `docs/releases.json`, `docs/CLAUDE.md`, `worker-positions/src/{positions,sweep}.js`, `worker-positions/{README,CLAUDE}.md`, `worker-positions/test/{positions,sweep}.test.js`, `tests/test_pwa_*.py`, `.github/workflows/tests.yml` | M | ✅ **Done (PR #320, branch `claude/any-ticker-entry-form-guwbl2`).** PWA form to open a position on any typed ticker (Positions tab, collapsed expander) — Option B "guided ticket" per the *Manual Position Entry* design review. Payload identical to picks path + `meta.source='manual'`, `stop_basis='manual'`. Refinements: bidirectional sizing (size by risk $ ↔ by shares), stop as price ↔ %-below-entry, optional `days_to_earnings`, optional **backdated `entry_date`** (validator accepts it, `buildPositionRow` uses it else stamps today). Lookup tab gets an "Open a position →" redirect that deep-links + prefills. `posCardHtml` already renders `source`/`manual` — zero list changes. **Review pass (2026-08-15) caught and fixed 4 issues before merge:** `releases.json` `current` not bumped to match the new entry (fixed by owner); `sw.js` `CACHE` not bumped despite the shipped `index.html` change (v66→v67); `state.manualEntry`'s init literal duplicated `manualEntryDefault()` field-for-field instead of calling it (deduped); and a real correctness bug — the backdated `entry_date` claim ("a backdate is a label, not a replay") wasn't actually enforced: `barWindowStart()`'s floor was only `max(last_advanced_date, entry_date)`, so a backdate onto a ticker with pre-existing `ticker_quotes` bars (global, un-scoped-by-position feed) would let the very next `sweep()` fold `advance()` over real pre-creation history in one shot — false stop-hits/trims off historical closes. Fixed by adding a third floor, `opened_at`'s ET trading date, which only ever binds when `entry_date` is backdated behind real creation (no-op otherwise). 6 new vitest (positions.test.js suite: 167 total in worker-positions). |
| WS5-8b | **Personal watchlist that rides the Morning scrape** (#319) | `worker-positions/**`, `scripts/{pick_status,collect_morning}.py`, `docs/**`, `tests/**` | L | 🟡 **DESIGN LOCKED — ready to build. Cold-start brief: `planning/watchlist-build-brief-8b.md`** (authoritative; supersedes §8b's think-big). Owner design session 2026-08-15 (PR #321, mocks `planning/mocks/ws5-watchlist-{directions,surface}.html`). Key reframe: NOT public-picks force-include — private D1 `watchlist` table (membership+level+TTL), union watch tickers into the **morning + held** feeds, ONE status engine (`pick_status.py`, no drift). Watch item ≠ ticket (no stop/size); §8a form reused only at **graduation**. Locked: privacy posture (a) anonymous-public quote; N=10 mornings; your-level = direction+quiet-met, no "crossed"; new `reclaim` engine state (today OR yesterday low); gauge on-by-default; level types above/below/20MA/50MA. Phasing **P1 worker/D1 ✅ (#322)** → **P2 feed+engine ✅ (#322 — `pick_status.py` reclaim + `collect_morning.py` union)** → **P3 PWA ✅ (this PR).** P3: Morning "Your watchlist" section (v2-mock card: no-header morning read, violet your-level block, on-by-default collapsible levels gauge, chart toggle, footer `N mornings left · I took it → · ⋯` kebab Renew/Edit/Remove), Positions "＋ Add to watchlist" collapsible (sibling to §8a manual-entry; ticker + optional above/below/20MA/50MA level), client-side your-level read (level_value never leaves owner-bearer path), graduation reuses §8a ticket then DELETEs the watch entry, `reclaim` added to `MORNING_STATUS_META`, picks list filters out `list_category='watchlist'` rows. Release triplet `2026.08.17`/sw.js v67→v68; 3 PWA display constants (3-places); `tests/test_pwa_watchlist.py` (+tests.yml ignore). Deferred (tracked in brief §10 + below): fully-private morning store, multi-day reclaim, picks opting into reclaim, **graduation entry-hint prefill from an above/below level (P3 nice-to-have, spec §4c-vi, not built)**, **signed-out public watch preview (if owner wants it)**, **inline `▾ Trade ticket`/level editor on the watch card (if owner wants them back)**. **P3 lead taste-calls flagged for owner:** signed-out watch section = sign-in prompt (no public preview); dropped the mock's separate `▾ Trade ticket` footer toggle (I-took-it is the sole graduation path); kebab Edit-level reuses the add form (re-POST upsert) not a bespoke editor; watch card uses "ATR from day low" copy (mock-faithful) vs picks' "ATR from LoD". |
| WS5-8b-OPS | ~~**Fix: wire POSITIONS secrets into `collect_morning.yml`**~~ | `.github/workflows/collect_morning.yml`, `tests/test_collect_morning.py` | XS | ✅ **Done 2026-08-20 (this PR, branch `claude/mornings-watchlist-processing-ll5vs1`).** Root cause of the "stuck on ADDING / 10 mornings left frozen" report: the "Collect morning status" step had **no `env:` block**, so `POSITIONS_WORKER_URL`/`POSITIONS_INGEST_TOKEN` never reached the runner. `collect_morning.py` gates BOTH the WS5-8b watchlist union AND the `/watchlist/tick` TTL decrement on `watchlist_configured = bool(url and token)` → every 10:05 ET run silently ran picks-only (green exit, no alert). Confirmed against live D1: all 8 watch tickers (added 8-17) still `sessions_remaining=10`, `watchlist_tick_log` empty, today's `morning_latest.csv` had zero `list_category='watchlist'` rows. Worker-side union (held feed) was unaffected — `ticker_quotes` did have the watch tickers. Fix mirrors the working `collect_held.yml`/`collect_held_preclose.yml` env blocks; added a regression guard test. **Closes the never-actioned 2026-08-15/08-16 session-notes TODO.** No backfill of the 8 tickers' TTL (they never lost sessions — start clean at 10 on next run). |
| WS5-8b-MONITOR | **Make the watchlist union failure loud, not silent** | `scripts/collect_morning.py`, `.github/workflows/collect_morning.yml` | S | 🔴 Backlog (raised 2026-08-20, non-blocking). The WS5-8b-OPS bug went unnoticed for days because a skipped union exits 0 with only a `print()`. Harden the closed loop: e.g. when `watchlist_configured` is False AND the run is a real (non-dry) scheduled run, emit a warning annotation / non-zero-ish signal, or a healthcheck ping, so "picks-only when it shouldn't be" surfaces. Consider a post-run assertion that if D1 has active watch items the morning store contains ≥1 `list_category='watchlist'` row. Ties into the deliberately-absent healthcheck noted in `collect_morning.yml`'s header (ADR-013 Decision 6). |
| WS5-HELD-TIMEOUT | **Held Feed 20-min timeout — Aug 19 run cancelled** | `.github/workflows/collect_held.yml`, `worker-positions/**` | S | 🔴 Backlog (found 2026-08-20 during the WS5-8b investigation, non-blocking). The 2026-08-19 `collect_held.yml` run was `cancelled` after ~21 min (hit `timeout-minutes: 20`) → no `ticker_quotes` bars for 8-19, so the advisory/reclaim MA refs have a one-day gap. 8-17/8-18 succeeded in ~1 min each, so the timeout is anomalous (Finviz scrape hang or worker POST stall), not the norm. Investigate the slow run's logs (run 32304257646) and decide whether to raise the budget or add a retry; a one-off cancel isn't urgent but a held scrape blowing its budget fails quietly like WS5-8b did. |
| WS-POSITIONS-STATUS | **Split "no_quote" into a real miss vs. "never had a bar yet"** | `worker-positions/src/watchlist.js`, `scripts/pick_status.py`, `scripts/collect_morning.py`, `docs/index.html`, `docs/{releases.json,sw.js}`, `tests/{test_pick_status,test_collect_morning}.py`, `worker-positions/test/watchlist.test.js` | M | ✅ **Done (PR #366, merged).** Design + staff review: `planning/watchlist-status-honesty-and-seeding.md`. Root cause (owner report 2026-08-25): a brand-new watch ticker's first `collect_morning.py` run (10:05 ET) always executes before that ticker's first `ticker_quotes` bar can exist (17:30 ET held feed), so it's tagged `no_quote` — copy "Morning feed missed this ticker" — which is false; nothing was missed, the read just ran too early relative to the ticker's own history. Fix: `/watchlist-tickers` gains `has_history:boolean`; `compute_pick_status()` gains an optional `has_history` param returning a new `STATUS_AWAITING_FIRST_READ` instead of `STATUS_NO_QUOTE` when it's `False` (default `None` for all picks callers — byte-identical existing behavior, same pattern as `ref`/`STATUS_RECLAIM`); `watchCardHtml()` splits the old two-state (no-bar / real-status) model into three, since a bar can land in D1 hours before the next `collect_morning.py` run has a chance to classify it. |
| WS-POSITIONS-SEED | **Seed a ticker's first bar on watchlist add (saves one trading morning)** | `worker-positions/src/{index,seed}.js`, `worker-positions/migrations/0006_ticker_quotes_source.sql`, `worker-positions/test/{seed.test,helpers/d1}.js` | M | 🟡 **BUILT — PR #368 (2026-08-26), owner-approved. Awaiting merge (apply migration 0006 to prod D1 out-of-band FIRST, then merge auto-deploys the worker).** Both residuals closed: Finviz takes displayed prices as-is via `collect_morning.py::fetch_ticker_quotes` (split-adjusted, matches FMP `full`); `refsFromRow()`/`watchlistTickerRefs()` join on `MAX(trade_date)` with no source-awareness, so a seeded bar is picked up transparently. FMP flat-array response shape re-verified live. 304 worker-positions tests pass (9 new). **Follow-up fix (2026-08-26, PR #368 review):** the seed call was `await`ed in `index.js` despite being documented fire-and-forget, adding up to `FMP_TIMEOUT_MS` (5s) of latency to every `POST /watchlist`. Fixed by threading `ctx` through `handleRequest`/`fetch` and handing the seed promise to `ctx.waitUntil()` instead of awaiting it inline; 1 new test pins the non-blocking behavior (311 worker-positions tests pass, was 310). Prior groundwork below. — **groundwork DONE, verdict GO (PR #367, 2026-08-25).** Full findings: `planning/watchlist-status-honesty-and-seeding.md` § "WS-POSITIONS-SEED — groundwork findings". All three prerequisites now satisfied at the research level, verified live: (1) endpoint is `GET /stable/historical-price-eod/full` (NOT `/stable/quote` — the running-quote endpoint the original review rejected), returns genuine completed prior sessions, split-adjusted, practically Finviz-scale over a 1-session lookback; (2) `0006_ticker_quotes_source.sql` = `ALTER TABLE ticker_quotes ADD COLUMN source TEXT NOT NULL DEFAULT 'finviz'`, confirmed additive/non-breaking against `ingestQuotes()`'s explicit `INGEST_COLS` + `sweep.js`'s `SELECT *` — and the trace showed `source` is **belt-and-suspenders, not a correctness requirement**: a past-dated seed is always outside `sweep.js`'s strictly-`>` window so it can never leak into a real position's advance; the sma50 level-vs-%-distance trap is sidestepped entirely by seeding OHLC-only and leaving `raw` empty; (3) no new job — the existing 15:30 ET `preclose_status` pass already reruns the full status engine, so a seeded ticker gets its first real read same-day. Ordered 5-step build plan in the planning doc. **Residual before merging the writer (not blocking start):** confirm Finviz's own OHLC adjustment convention (`collect_held.py`) and `refsFromRow()`'s read query. Realistic ceiling: saves one trading morning, not "instant." |
| WS-POSITIONS-MONITOR | **Watchlist union health check — positive assertion, not just a warning** | `scripts/collect_morning.py`, `.github/workflows/collect_morning.yml` | S | 🔴 Backlog — refines/supersedes `WS5-8b-MONITOR` above (same underlying gap, scope corrected 2026-08-25 per staff review). A stderr-warning-and-nonzero-exit design has the same silent-failure shape as the original `WS5-8b-OPS` incident — a step that never runs (missing secret) exits 0 and nobody notices, again. Build the healthchecks.io dead-man's-switch FIRST (mirrors the existing `collect_picks.yml` pattern), pinged only when a post-run check asserts something positive (e.g. "N active watch tickers, M resolved to a real level or a genuine `no_quote` — zero stuck on `awaiting_first_read` past 1 session"). Depends on `WS-POSITIONS-STATUS` landing first — the check is meaningless while `no_quote` still conflates "never had data" with "actually missing." |
| WS-POSITIONS-TTL-BURN | **`sessions_remaining` decrements even on a day with zero real read** | `worker-positions/src/watchlist.js` | XS | ✅ **Done (PR #367, 2026-08-25).** Owner chose "skip the decrement until first real read." `tickWatchlist()`'s step-3 UPDATE is now scoped `WHERE status = 'active' AND ticker IN (SELECT ticker FROM ticker_quotes)` — an `awaiting_first_read` row (no bar yet) stays at full TTL until its first EOD bar lands, so a weekend add no longer burns "mornings" delivering zero info. Added a `skipped_no_history` count to the tick response (observability + the natural input to WS-POSITIONS-MONITOR). 3-places docs updated; 295 vitest green. |
| WS-MORNING-SUBTABS | ~~**Morning tab: Picks/Watchlist subtabs, levels-hidden default, soft-remove**~~ | `worker-positions/{src/watchlist.js,migrations/0007_watchlist_removed.sql,test/watchlist.test.js,test/helpers/d1.js,README.md,CLAUDE.md}`, `docs/{index.html,releases.json,sw.js,CLAUDE.md}`, `tests/test_pwa_watchlist.py` | M | ✅ **Done (this PR, branch `claude/morning-subtabs-watchlist-q70cvm`).** Owner request (no issue #). Three pieces: (1) **Subtabs** — Morning tab split into `#morning-subtab-picks` / `#morning-subtab-watchlist` panes behind a segmented-pill nav (`renderMorningSubtabs()`/`__setMorningSubtab`), default `'picks'`, both panes render unconditionally so switching is a pure visibility toggle, no re-fetch. (2) **Levels gauge defaults hidden** — flipped `state.watchGauge[ticker]`'s polarity from "collapsed?" to "shown?" (absent/false = hidden now), owner request. (3) **Soft-remove** — new `watchlist.status='removed'` (migration 0007 adds `removed_at`), `PATCH /watchlist/:id {remove:true}`/`{restore:true}` (`watchlist.js::patchWatch`), replacing the old hard `DELETE` for the kebab's Remove action (DELETE kept, now graduation-only). `watchlistTickers()`'s existing `status='active'` filter already excludes `removed` identically to `expired` — the held-feed scrape and public `/watchlist-tickers` feed stop covering a removed ticker with no new filter. PWA renders a collapsed "Recently removed" bin (mirrors Expired) with a free TradingView chart + Restore per card. `tickWatchlist()` purges `removed` rows after `WATCHLIST_PURGE_DAYS`, symmetric with `expired`. 310 vitest (was 295: +6 new, +9 total incl. 0007 migration coverage); 719 pytest (non-Playwright) unchanged; 4 new/updated Playwright watchlist tests verified functionally via the revision-symlink harness (real Chromium run in this session, not just code review) — all pass. Release `2026.08.26.2`, sw.js v84→v85. **Backend needs the same out-of-band migration step as WS-POSITIONS-SEED (#368):** apply `migrations/0007_watchlist_removed.sql` to prod `finviz-positions` D1 before/alongside merge. |

| B6-CARD-STANDARDIZE | ~~**Morning/Watch card standardization: ATR-from-LoD + Rel volume on every status; stale-trailing-window caveat**~~ | `scripts/{pick_status,collect_morning}.py`, `docs/index.html`, `docs/CLAUDE.md`, `README.md`, `tests/{test_pick_status,test_collect_morning,test_pwa_morning,test_pwa_watchlist,test_pwa_picks_atr_earnings}.py`, `.session/SPRINT.md` | S | ✅ **Done 2026-09-02.** Owner-requested follow-up to B-6 (§ Compression "Volatility & setup" section). Three pieces: (1) **Caveat copy** — the B-2/B-3 sub-blocks inside `volSetupSectionHtml()` (Range/Volume trailing-window sparklines, sourced from the picks_latest cross-ref, one session behind the fresh B-1 row above them on Morning/Watch cards) renamed "Range tightening"/"Volume dry-up" → "Range over last 10 sessions"/"Volume over last 10 sessions", plus a conditional "as of last close M/D" clause (`opts.staleTrailing`, threaded from `morningCardBody`/`watchCardHtml`; the Picks tab passes no opts since there the whole card is same-EOD-run, no lag to caveat). Renamed headers apply everywhere (shared render fn, clearer copy); the "as of" clause is Morning/Watch-only. (2) **ATR-from-LoD + Rel volume standardized onto every card status**, not just actionable ones (owner: "pretty much free" — `compute_atr_from_lod` was already a pure unconditional fn; the `ACTIONABLE_STATUSES` gate in `collect_morning.py` was the only thing wrapping it, removed) — same color bands (owner: keep them) on failed_breakout/setting_up/invalidated too; the trade-ticket/"I took it" CTA stays actionable-only (unrelated gate). (3) **Fixed the pre-existing `watchCardHtml` reclaim-branch inconsistency** flagged in review — that branch was a separate hand-built body predating the shared atrRow/rvolRow rows and never rendered them (not a deliberate design choice, just never backfilled); now every watch card state (reclaim included) renders the same two rows. Backend + frontend covered by tests; 120 Playwright + 737 non-Playwright pass. |

#### Data Pipeline

| # | Task | File(s) | Effort | Notes |
|---|------|---------|--------|-------|
| WIDE-SCRAPE-FASTFOLLOW | **Scope unused wide-scrape columns unlocked by PR #384** (#385) | `scripts/collect_morning.py` | S | 🔴 Backlog (PR #384 review finding, 2026-09-01). The morning/pre_close scrape is now the 84-col `held` block, but only 5 columns (`SETUP_COLUMNS`) are carried into the store. SMA20/50/200, 52W Low, Beta, Gap, Change from Open, Earnings date, EPS/Revenue Surprise, Recom, Target Price, News are already on the wire at ~zero extra cost and currently discarded. **Not scoped to card work alone** — Earnings/EPS-Surprise and the SMA columns are equally relevant to WS4's trade-ticket earnings guardrail and WS5's `advance()` engine, independent of the Effort A card rework (#378/A-2/B-6). See `planning/compression-expansion-ideation.md` § Cross-cutting follow-ups. Not urgent — extending `SETUP_COLUMNS` is a one-line change once a consumer needs one of these fields. |
| CHORE-1 | **Remove stale `Infrastructure Operations` references** | `data/finviz_sector_industry_map.json`, `data/finviz_sector_industry_map.csv`, `tests/test_seed_taxonomy.py`, `CLAUDE.md` | XS | Finviz no longer carries `Infrastructure Operations`. The picks pipeline slug map (built from `snapshots.csv`) is already clean (144 rows, excludes it). These four taxonomy/seed files still reference it. Clean up in a standalone PR — scoped separately from Phase 2 to avoid touching seed_taxonomy validation during an unrelated implementation sprint. |
| PIPE-1 | **Backfill historical deltas.csv after day-removal formula change** | `data/sectors/deltas.csv`, `data/industries/deltas.csv`, `scripts/compute_deltas.py` | S | `momentum_score` and `rs_score` rows before PR #150 were computed with 7 timeframes (including day); rows after use 6. The `momentum_accel` and `rs_accel` deltas will straddle the discontinuity for ~10 sessions after merge. Fix: run `compute_deltas.py` over all historical dates to recompute from scratch. Do this in a separate PR (don't bundle with the formula change) to keep diffs legible. |
| ~~TAX-0~~ | ~~**Seed sector→industry taxonomy map**~~ | `scripts/seed_taxonomy.py`, `tests/test_seed_taxonomy.py`, `data/finviz_sector_industry_map.{json,csv}` | S | ✅ **Done 2026-06-24.** Replaced PR #109's Playwright/Cloudflare plan with a plain-HTTP parse of fasiha/finviz-git-scraper's `map-sec_all.json`. 11/11 sectors, 144/144 industries match (100%). 1 extra in fasiha (`Infrastructure Operations`) — newer Finviz addition. PR #109 superseded and should be closed. 13 tests pass. |
| ~~INS-7~~ | ~~**Sector Breadth metric**~~ | `dashboard/app.py`, `dashboard/sector_breadth.py`, `tests/test_sector_breadth.py` | M | ✅ **Done 2026-06-24.** Sector Breadth table in Strength tab (Industries view): for each sector, count how many industries rank in top half of full universe. `compute_sector_breadth()` extracted to `dashboard/sector_breadth.py` (10 tests). Rank metric selectbox (week/month/ytd). Full-universe breadth unaffected by sidebar sector filter. PWA breadth bar (Feature B) deferred to Phase 2. |
| ~~TASK-6B~~ | ~~**Streamlit sidebar sector filter**~~ | `dashboard/app.py` | S | ✅ **Done 2026-06-24.** Sidebar selectbox "Sector" (All + 11 sectors) when viewing Industries. Filters snap_df, delta_df, snap_df_full, delta_df_full by selected sector's industries in all tabs. Full-universe copy preserved for INS-7 breadth computation. |
| ~~HIE-2A~~ | ~~**PWA sector breadth bar + expand-in-place drill-down (Phase 2)**~~ | `docs/index.html`, `docs/sw.js`, `docs/releases.json`, `knowledge/moaty-metrics.md`, `planning/PLAN_sector_industry_hierarchy.md` | M | ✅ **Done 2026-06-24.** Sector cards in Today tab show fill bar + "N/M industries top-half" count. Tap sector card to expand inline drill-down listing constituent industries sorted by YTD rank. `TAXONOMY_URL`, `fetchJSON()`, `computeSectorBreadth()`, `breadthBar()`, `renderSectorDrillDown()`, `loadTaxonomyAndBreadth()` added. GUIDE `sector_breadth` metric + moaty-metrics.md entry. Release triplet: 2026.06.24.1, sw.js CACHE → v30. VP decisions: expand-in-place (Feature A), count+mini-bar (Feature B), drill-down only (Feature F). |
| HIE-FF1 | **[FAST-FOLLOW] PWA sector filter** | `docs/index.html` | M | Analogous to Streamlit TASK-6B. Filter Today/Momentum/Strength tabs to show only one sector's industries. Requires a sector chip strip UI element. Plan: add sector chips above the main group list when `state.group === 'industries'`. |
| HIE-FF2 | **[FAST-FOLLOW] Sector rank chip on collapsed card** | `docs/index.html` | S | Feature F (VP deferred to drill-down only in Phase 2). Add "#N in Sector" chip to the collapsed sector card view. Evaluate after getting user feedback on the drill-down behavior first. |
| DATA-FINVIZ-LOW | **[WATCH] Possible Finviz scraped-`Low` data-integrity gap** | `scripts/collect_morning.py`, `scripts/pick_status.py`, `data/picks/sessions/morning*.csv` | — | Owner report 2026-08-28: RPRX's Morning-tab card showed "ATR from LoD 0.1 — ok to act" that looked implausibly clean; the ticket's "Today low" read **61.42**, but the owner's broker chart showed the actual day low at **61.01**, printed in the very first 5-minute bar (9:30–9:35 ET) — well before the `collect_morning.py` scrape at 10:07:32 ET (`morning.csv` row: `2026-08-27,morning,2026-08-27T14:07:32Z,RPRX,...,open=61.44,high=61.67,low=61.42,price=61.6,...,reclaim,...`). Verified this is **not** a parsing/column-mapping bug on our side: `screener_config.json`'s `morning` block id 88 → label "Low" matches `tests/test_collect_morning.py`'s fixture header, and `compute_atr_from_lod`'s math on the stored `61.42` is correct (`(61.60−61.42)/1.35=0.133→"0.1"`). So either (a) Finviz's own delayed-quote `Low` for this narrow `t=`-filtered screener view understated the true tape at scrape time (external data lag/quirk, outside our control), or (b) something about how/when we request this specific block causes a stale read even 37 min after the open. Can't confirm which from this cloud sandbox — Cloudflare blocks live Finviz access from here (root `CLAUDE.md` § Playwright). **Mitigated, not fixed**, by WS4-LOW above (a manual low-override input in the ticket) — that only lets the user correct a wrong reading after the fact, it doesn't explain or prevent the root cause. **Next step for whoever picks this up:** watch for a repeat (cross-check a few more `reclaim`/`triggered` mornings' `low` column against an independent source — e.g. finviz.com's own quote page for that ticker near the scrape's `collected_at`, or a broker/TradingView chart) to determine whether this is a one-off Finviz data glitch or a systematic lag specific to the `morning` scrape block. If systematic, consider widening `MORNING_BATCH_SIZE`'s request timing or adding a sanity cross-check against `high`/`open`/prior-day data. |

---

#### Stock Picks Pipeline

Full plan: `planning/stock-picks-from-leading-groups.md` · ADR-007 (selector) · ADR-008 (architecture)

| ID | Task | Files | Est | Status |
|----|------|-------|-----|--------|
| PICKS-SEL-V3-PWA | ~~**PWA: label/color the new `all_green` list_category (selector v3)**~~ | `docs/index.html` (`CATEGORY_LABEL`, `CAT_LABELS`, `catColor`/`CAT_COLORS` maps, `catOrder`/`CAT_ORDER` arrays — search `list_category`), release triplet | S | ✅ **Done 2026-08-25.** Added `all_green` → `'All Green'` + green chip class to all 5 map/array pairs (`CATEGORY_LABEL`/`CATEGORY_CHIP_CLS`, `catOrder`, `CAT_ORDER`/`CAT_LABELS`/`CAT_COLORS`, and `ws4PickCategories`'s display-order array). Bumped `display_methodology.json` to v5 (`all_view_sort.category_order` now includes `all_green`) since `test_picks_methodology.py`'s anti-drift guard checks `CAT_ORDER` against it; updated the two hardcoded-version tests in `test_replay_picks.py` that assumed v4 was latest. Release triplet (`releases.json` 2026.08.25 + `sw.js` v81) shipped in the same PR. |
| OVERHEAD-1 | ~~**Launch-ready overhead chip (Phase 1)**~~ | `docs/index.html`, `knowledge/moaty-metrics.md`, release triplet | S | ✅ **Done 2026-08-12, PR #303 merged.** Display-only Coiled/Extended/Overhead chip per Picks row via `computeLaunchReady()`; `LAUNCH_NEAR_HIGH_PCT/_CALM_EXT_MAX/_OVERHEAD_PCT` from `52W High` + `atr_ext_50`. |
| OVERHEAD-2 | **Focus overhead penalty (Phase 2)** | `docs/index.html`, `scripts/replay_picks.py`, `data/picks/display_methodology.json`, `tests/test_picks_methodology.py`, `tests/test_replay_picks.py`, `docs/CLAUDE.md`, release triplet | M | 🟡 **In review.** `score *= (1 − overheadPen)`, `overheadPen = 0.20 × clamp((ohMag−8)/(30−8),0,1)`. Mirrored JS/Python/JSON (methodology v4). **Process note:** Phase 2 was first pushed onto the already-merged #303 branch (stranded); re-based onto latest default + new PR. **Review follow-up (same day):** added `TestOverheadPenaltyFrac` + `TestReplayV4Fixture` (`tests/fixtures/replay_picks_v4_fixture.csv`) closing a test-coverage gap vs. the sibling liquidity/earnings penalty tests; confirmed + documented that a Finviz data-lag positive `52W High` (owner-observed) is already handled gracefully by the existing clamp in both P1 (`computeLaunchReady`) and P2 (`overheadPenaltyFrac`), no functional fix needed — only a misleading "always ≤ 0" doc comment corrected. |
| OVERHEAD-3 | **Raise `OVERHEAD_PENALTY_MAX` 0.20 → 0.30** | `docs/index.html`, `data/picks/display_methodology.json` (new version), guard tests, release triplet | XS | 🔴 Backlog. Deferred deliberately — ship the conservative 0.20 tiebreaker, observe the Focus reshuffle over a few sessions, then decide. Bump is a one-liner + methodology version + anti-drift test update. |
| OVERHEAD-4 | ~~**Surface overhead signal in Lookup tab**~~ | `docs/index.html`, release triplet | S | ✅ **Done 2026-08-14.** Chip added to the existing "This stock" ticker-context card (`findTickerPickInfo()`/`tickerContextHtml()`) via `computeLaunchReady()` on the row already resolved from `state.picksData` — no new lookup path. **GH #304.** |
| OVERHEAD-5 | ~~**Surface overhead signal in Morning tab (chip)**~~ | `docs/index.html`, release triplet | S | ✅ **Done 2026-08-14, chip-only pass (owner-approved).** Resolved the "open question": `morning_latest.csv` genuinely lacks `52W High`/`atr_ext_50`, but `ws4FindPicksRow()` (already built for the WS4 trade ticket) already joins to the prior-EOD `picks_latest` row that has them — reused as-is, no morning-store schema change. Chip rendered next to the ticker in the card header. **e2e tests deliberately deferred** (owner call) — folding into OVERHEAD-6's fast-follow PR instead of adding now, then removing. **GH #305** (partial — chip only; sort/filter is OVERHEAD-6). |
| OVERHEAD-6 | **Morning tab: overhead-aware sort/filter (fast-follow to OVERHEAD-5)** | `docs/index.html`, release triplet, e2e tests | S–M | 🔴 Backlog. #305 raised sort/filter de-emphasis as an option beyond the chip; owner asked for several option mockups (incl. one reusing/adapting the Picks-tab top filter bar) before picking a direction. Covers the e2e test coverage deferred from OVERHEAD-5 too. |

| # | Task | File(s) | Effort | Notes |
|---|------|---------|--------|-------|
| PICKS-FIX-A | **🔴 Picks tickers single-letter — fix parser** (#252) | `scripts/probe_picks.py`, `tests/test_probe_picks.py` | S | **In progress.** PR #248's `find("a")` grabs the avatar anchor, not the ticker. Read ticker from canonical `quote.ashx?t=SYMBOL` href instead of positional anchor text; fall back to anchor text then cell text. Add fixtures for avatar-as-`<a>` (the real prod shape) + avatar-as-`<span>` (old fixture). |
| PICKS-FIX-B | **🔴 Harden ticker-corruption guard for single-char** (#252) | `scripts/collect_picks.py`, `tests/test_collect_picks.py`, `README.md` | S | **In progress.** `ticker_dup_rate` blind to 1-char tickers (`len<2`). Add `single_char_ticker_rate()` + `TICKER_SHORT_RATE_MAX=0.30` (healthy ~1.3%, corrupt 100%); abort before write if exceeded. Triple-document the constant. |
| PICKS-FIX-C | **🔴 Picks stale since 07-17 — CF picks cron never deployed** (#252) | `worker-cron/wrangler.toml`, `.github/workflows/collect_picks.yml`, Cloudflare account | S | deploy-workers run #8 failed: Cloudflare 5-cron-trigger account limit `[code:10072]` — worker code deployed but `31 22` picks cron never registered, and GitHub `schedule:` was removed. Raise limit / consolidate crons, redeploy, verify triggers; interim restore a GitHub `schedule:` backstop. **Blocked-by: A+B must land first** or resumed runs write more corrupt data. |
| PICKS-FIX-D | **Repair 07-16/07-17 corrupt ticker rows** (#252) | `data/picks/picks.csv`, `data/picks/picks_latest.csv` | S | **Deferred / non-blocking.** Two-way door: Company/Industry/metrics intact, tickers reconstructable from Company anytime; only a fresh re-scrape is unrecoverable (already captured). Do after A+B+C. |
| ~~PICKS-2~~ | ~~**Phase 2: collect_picks.py + selector + workflow**~~ | `scripts/collect_picks.py`, `scripts/picks_config.py`, `data/picks/selector_versions.json`, `.github/workflows/collect_picks.yml`, `.github/workflows/collect.yml`, `tests/test_collect_picks.py` | L | ✅ **Code complete 2026-06-25 (branch `claude/brave-hypatia-pfzhu1`).** Pure `select_groups()` (4-bucket priority-fill, ≤20 unique, dedup-tagged, 19 `grp_*` snapshot), paginated scrape (`PAGE_CAP`/`GLOBAL_FETCH_CAP=50`), append/dedup + `picks_latest.csv`, G4 `validated` flip, stale-read guard. Shared `concurrency: finviz-data-commit` on BOTH workflows (G1). v1 registry w/ immutability tests. 30 tests, 480 pass. **Remaining: one live Actions dispatch to start daily capture + confirm 84-col scrape on Azure.** |
| ~~PICKS-2-LIVE~~ | ~~**Dispatch collect_picks.yml — start the daily clock**~~ | — | XS | ✅ **Done 2026-06-25.** First green run: 273 picks / 262 unique tickers / 19 industry groups / all 4 buckets populated / 19/19 slugs validated / dedup confirmed (Packaging & Containers in both emerging + accel). `picks_latest.csv` correct (max-date slice = full CSV since day 1). 108 cols per row (84 Finviz + 5 lead + 19 grp_*). |
| ~~PICKS-2-ADR8~~ | ~~**Reconcile ADR-008 grp_* table with the 19-col plan spec**~~ | `knowledge/decisions/ADR-008-picks-collection-architecture.md` | XS | ✅ **Done 2026-06-25.** ADR-008 updated to match the implemented 19-col spec: replaced `grp_selection_priority` with `grp_category_rank`; added `grp_momentum_weighted_mid`, `grp_rank_agreement`, `grp_rs_accel`. Added reconciliation note explaining the drift from the initial draft. |
| ~~PICKS-2-HDR~~ | ~~**Validate scraped Finviz header against `finviz_cols` at scrape time**~~ | `scripts/collect_picks.py`, `tests/test_collect_picks.py` | S | ✅ **Done 2026-07-19.** `missing_header_labels()` (union across scraped group headers; empty-header wrong-slug groups excluded) + `header_check_action()` tiered policy: `Ticker` missing or > `HEADER_MISSING_ABORT_FRAC` (0.10 of 84 ≈ 8 labels) missing → abort BEFORE write (exit 1, CI red, debug HTML uploads); smaller drift → write the partial capture (bounded column loss beats losing the irreplaceable day) then exit 1 AFTER the write so CI reds and the success-only healthcheck ping is skipped. 9 unit tests in `TestHeaderDriftGuard`. Triple-documented (in-code, README § Configurable parameters, scripts/CLAUDE.md). |
| ~~PICKS-2-CRON~~ | ~~**Promote collect_picks.yml to the Cloudflare-cron dispatcher (timing robustness)**~~ | `.github/workflows/collect_picks.yml`, `worker-cron/wrangler.toml` | S | ✅ **Done 2026-07-19** per planning/cloudflare-cron-scheduler.md Phase 5 (one correction: deployed crons use `2-6` day-of-week — Cloudflare's 1=Sunday convention — not the plan's `1-5`). New CF cron `31 22 * * 2-6` (6:31 PM EDT, EOD +90 min); `scheduled()` routes by exact `event.cron` match (`workflowForCron`), separate KV keys `last_dispatch_collect`/`last_dispatch_picks`, `/last` returns `{collect, picks, legacy}`. GitHub `schedule:` REMOVED from collect_picks.yml by design (50-page scrape too expensive for a drifting backstop); success-only `PICKS_HEALTHCHECK_URL` dead-man ping added (skips silently until the VP creates the secret — still an open VP action item). 20 worker tests pass. |
| PICKS-3 | **Phase 3: PWA surfaces (parent — SPEC LOCKED 2026-06-26)** | — | L | **Spec locked, CEO-aligned.** See plan §"Phase 3 — PWA surfaces (DETAILED SPEC)" — subphases below (3a→3d), acceptance criteria + tests in the plan. Decisions: backend derives raw metrics / PWA holds thresholds (C5); base filter >5B + MA cond (C6); Focus = blended quality rank, no 50MA-proximity reward (C7); no RSI gate (C8); fundamentals + honorable-mentions deferred to 3d (C9). |
| PICKS-3A | **3a: Picks tab MVP + backend derived metrics** | `scripts/picks_metrics.py` (new), `scripts/collect_picks.py`, `data/picks/picks*.csv`, `tests/test_picks_metrics.py`, `docs/index.html`, `docs/sw.js`, `docs/releases.json` | M | Backend cols `atr_ext_50, risk_20ma_pct, risk_50ma_pct, range_atr, stage2` via `ensure_picks_csv()` migration + new Picks tab: base filter (C6, ~141 EOD rows), category→industry→stock, least-extended-first, extension color bands (C4), breadth count. WELCOME/VALID_TAB_IDS/GUIDE_TAB_CHIPS updates + vsmarket gap fix. Release triplet. Worked-example assertions (EOD): ANET≈0.67×, STX≈3.16×, DELL≈3.64×, SNDK≈4.55×. |
| ~~PICKS-3B~~ | ~~**3b: Risk engine + Focus List toggle (SPEC REFINED 2026-06-27)**~~ | `docs/index.html`, `docs/sw.js`, `docs/releases.json`, `tests/test_pwa_picks.py` (NEW), `tests/fixtures/picks_latest.csv` | M | ✅ **Done 2026-06-27.** `renderPickRow()` extracted as top-level helper; expandable risk panel with HoD, 20MA/50MA stops (price/$/%/sh), extension, Range/ATR, Volatility(ATR%), Stop distance(ATR), score debug breakdown. Focus scoring: one min–max ruler, multiplicative discount, nearest-positive-MA stop (TESTAB20 regression row added). All/Focus toggle, A4 reset. Release triplet v2026.06.27. 6 new constants triple-documented. 519 tests pass. `tests/test_pwa_picks.py` created with 13 Playwright tests. **A4 (reset-to-All) reversed 2026-06-30 — see PICKS-STATE-PERSIST.** |
| ~~PICKS-3C~~ | ~~**3c: Lookup Stage-2 section + deep-link button**~~ | `docs/index.html`, `docs/sw.js`, `docs/releases.json`, `tests/test_picks_button_config.py` (NEW) | S | ✅ **Done 2026-06-28.** Stage-2 section added to both `renderLookup()` branches. `slugifyGroup()` + `buildScreenerUrl()` helpers. `BUTTON_*` constants inlined (BUTTON_V, BUTTON_BASE_FILTERS, BUTTON_SORT, BUTTON_FT). `ind_<slug>` button for all 144 industries, `sec_<slug>` for all 11 sectors. 9-test anti-drift guard (`tests/test_picks_button_config.py`). Release triplet v2026.06.28 (sw.js → finviz-v33). Triple-documented. 478 tests pass. |
| ~~PICKS-3E~~ | ~~**Phase A: HoD price-basis toggle (per-card, display-only)**~~ | `docs/index.html`, `docs/sw.js`, `docs/releases.json`, `tests/test_pwa_picks_hod.py` (NEW), `tests/fixtures/picks_latest.csv`, `knowledge/moaty-metrics.md` | M | ✅ **Done 2026-06-30.** `deriveRiskMetrics(row, basis)` pure function; `__buildRiskBasisContent(rowData, basis)` renders basis-dependent HTML; `__setPickBasis(key, basis)` updates button states + re-renders content; `__togglePickRow` resets basis on collapse (ephemeral state rule). `[ Last \| HoD ]` toggle buttons inside every expanded risk panel. trim→extended label in HoD mode. `price_basis` GUIDE entry + moaty-metrics.md entry. TESTHOD fixture row added. 5 Playwright tests in `test_pwa_picks_hod.py`. Release triplet v2026.06.30 (sw.js → finviz-v36). Phase B (global Focus re-rank) tracked below. |
| ~~PICKS-3E-HOD-PHASE-B~~ | ~~**Phase B: Global HoD toggle re-ranks Focus list**~~ | `docs/index.html`, `docs/sw.js`, `docs/releases.json`, `tests/test_pwa_picks_hod.py`, `knowledge/moaty-metrics.md` | L | ✅ **Done 2026-07-04.** `state.picksBasis` ('last'\|'hod', default 'last') + `[ Last \| HoD ]` header toggle next to All/Focus. `renderPicks()` derives every displayed row via `{...r, ...deriveRiskMetrics(r, state.picksBasis)}` (zero-mutation spread, per plan §6.2) before the Focus hard gate, `computeFocusScores`, the All-view sort, and the pre-scored All-view badge map — same `dr` row flows into `renderPickRow` so collapsed-row badges pick up the global basis with no extra code. Per-card toggle (Phase A) now defaults to the global basis on open and reverts to it (not hardcoded 'last') on collapse — a local override for peeking at one name on the other basis, per §6.3. 6 new Playwright tests in `test_pwa_picks_hod.py` (button render/default, Focus-gate exclusion under HoD, collapsed-badge update, per-card default-to-global, collapse-reverts-to-global, All-view sort reorder) — 11 total in that file, all pass. `price_basis` GUIDE entry + moaty-metrics.md updated for Phase B. Release triplet v2026.07.04.1 (sw.js → finviz-v55). No new configurable constants (existing ATR_EXT_*/FOCUS_W_* reused per plan §10). |
| ~~PICKS-CHARTS~~ | ~~**Finviz charts-grid deep-links (v=211 multi-ticker view)**~~ | `docs/index.html`, `docs/sw.js`, `docs/releases.json` | S | ✅ **Done 2026-06-30.** `buildChartsUrl(tickers)` (dedupes via Set, no cap). "Charts ↗" links added: per-group header in Picks All view, tab-level "View all N charts in Finviz" in All + Focus views, and beside the Stage-2 screener button in Lookup. Also fixed 2 internal nav-to-Lookup buttons that incorrectly used `↗` (external-link convention) instead of `›` (internal convention) — per-group header button + Focus/Lookup row subtitle button. Scroll position retention added (`state.scrollPos`, restored in `render()`) so tabs keep their scroll position on re-entry. Release triplet v2026.06.30.5 (sw.js → finviz-v41). Verified end-to-end with a real headless Chromium session (fixture-intercept pattern) — confirmed dedup, `›` vs `↗`, and scroll restore all work. |
| ~~PICKS-STATE-PERSIST~~ | ~~**Retain Picks tab expanded-row + All/Focus view state across tab navigation**~~ | `docs/index.html`, `planning/stock-picks-from-leading-groups.md`, `docs/sw.js`, `docs/releases.json` | M | ✅ **Done 2026-06-30 (explicit VP call to reverse A4).** (1) `state.picksExpanded` (Set of stable `ticker_category` keys) persists which risk panels are open; the check lives inside the shared `renderPickRow()` helper, so it also applies to the Lookup tab's Stage-2 section (`renderLookupStage2()`) for free — both call sites use the same function and the same global state, not just the Picks tab. `__togglePickRow(key, expandKey)` updates the set on toggle from either call site. (2) `switchTab()` no longer forces `picksView` back to `'all'` — the All/Focus selection now survives tab navigation. Scroll-save skip-on-Focus logic (added for PICKS-CHARTS) removed since it's no longer needed. A4 reversal documented in `planning/stock-picks-from-leading-groups.md` (note appended, not rewritten) + the `state.picksView` code comment + the All/Focus toggle HTML comment. Per-card price-basis stays ephemeral (resets on collapse) — out of scope for this reversal, that's a separate, intentional design (see `price_basis` GUIDE entry). |
| PICKS-3D | **3d: Polish (optional)** | `scripts/collect_picks.py`, `docs/index.html` | M | True inside-day (prev-day H/L self-join, schema bump) replacing the C1 proxy; loose fundamental floor on Focus + "Honorable mentions (failed fundamental floor)" sub-list (C9); search/filter, sort toggles, target/R framing. Also contains PICKS-3D-STALE + PICKS-3D-STACKEDSTOP (see below). |
| PICKS-3D-MOBILE | **[FAST-FOLLOW] 8-tab flex-1 mobile viewport check** | `docs/index.html` | XS | After adding Picks (8th tab), eyeball the tab bar at 375px (iPhone SE) viewport. 8 × `flex-1` items at 375px = ~47px each — "Picks" (5 chars) fits but is tight. If any label truncates or wraps, abbreviate or shorten. Test at 375px before marking 3a done. |
| PICKS-3D-STALE | **[FAST-FOLLOW] Intraday staleness banner + run_at column** | `scripts/collect_picks.py`, `data/picks/picks*.csv`, `docs/index.html` | S | Add a `run_at` column to `picks.csv` (ISO UTC timestamp stamped at write time in `collect_picks.py`). In the PWA, if `run_at` is < 16:00 ET on the data date, show a subtle banner: "Prices/High captured intraday — trigger levels valid for next session after EOD update." Our cron fires EOD so this is a no-op in normal operation; only fires if someone manually dispatches the workflow earlier in the day. |
| PICKS-3B-FOCUSGATE | **[POST-LIVE] Revisit Focus gate: price>50MA vs full stage2** | `docs/index.html` | XS | 3b admits any Focus name above its 50MA; does NOT require 50MA>200MA. After a few weeks of live Focus data, decide whether sub-Stage-2 names pollute the list and the gate should tighten to `stage2==1`. Also revisit whether freshness-fill leaders should get `momentum_confirmed` credit in the Focus group-strength component (M3). Tracked per CEO 2026-06-27. |
| PICKS-3B-FOCUSTEST | **[FAST-FOLLOW] Deterministic Focus-order regression test** | `tests/test_pwa_picks.py` | S | Freeze a small fixture pool, assert whole-pool ordering + scores within ±0.01 (a single stock's score can't be pinned in isolation — cross-sectionally normalized). Tabled per CEO 2026-06-27; 3b acceptance test asserts qualitative properties (scores∈[0,1], discount observable, below-MA excluded) instead. |
| PICKS-3C-PLAYWRIGHT-GAP | **[FAST-FOLLOW] Playwright tests for Lookup Stage-2 section** | `tests/test_pwa_picks.py` or new `tests/test_pwa_lookup_stage2.py` | S | Phase 3c shipped no Playwright tests for the new `renderLookupStage2()` function (only Python anti-drift tests in `test_picks_button_config.py`). A regression in the Lookup tab rendering — category chips, picks list, screener button URL, empty-state message — would not be caught by CI. Pattern: intercept `picks_latest.csv` and `industries/deltas.csv` with fixture data, look up a group name, assert the Stage-2 section renders. Specific gaps: (1) industry with picks → picks list + category chip appear; (2) industry NOT in picks → empty-state message appears + button still renders; (3) sector lookup → no picks list, only sector button; (4) ticker lookup (both branches) → industry section + sector button; (5) screener button href contains correct `ind_<slug>` token. Use same http.server + Playwright intercept pattern as `test_pwa_picks.py`. Prerequisite: `playwright install chromium`. |
| PICKS-3D-STACKEDSTOP | **[3d POLISH] 50MA double-support bonus on Focus score** | `docs/index.html` | S | Add the "both MAs tight AND close together" reward (`|sma20_price−sma50_price|/price` below threshold = two stacked supports) on top of the v1 nearest-stop component. v1 already gives the "either MA" reward; this adds "both, and close." See plan §3d. |
| ~~PICKS-4~~ | ~~**Phase 4a: group-level alpha scoreboard (`evaluate_picks.py`)**~~ | `scripts/evaluate_picks.py`, `tests/test_evaluate_picks.py`, `data/picks/eval/group_scores.csv`, `.github/workflows/collect.yml` | M | **DONE 2026-07-19.** Built per `planning/picks-alpha-evaluation.md` Part 1 with three deliberate amendments: (1) `group_scores.csv` is a **derived, fully-rebuilt** artifact (not append-only/LWW — partial rows self-correct for free); (2) added the non-selected control columns (`n_nonsel`, `fwd_ret_nonsel_mean`, `excess_nonsel`) since the spec's own paired per-date test wasn't computable from the spec'd schema; (3) **wired into `collect.yml` as a step after `compute_deltas.py`** instead of a third data-writer workflow — rides the existing `finviz-data-commit` group + commit (also added the AUD-4 `git pull --rebase` to collect.yml's push). `ticker_scores.csv` deliberately NOT built (survivorship-biased until real OHLC — see PICKS-4B). First real build committed: 15 pick dates, leaders-drag/rotation-edge pattern from the 07-14 assessment holds and sharpens at h=10. |
| PICKS-4B | **Phase 4b: stock-level scoreboard + R-multiple expectancy (needs real OHLC)** | `scripts/evaluate_picks.py`, `worker/` (FMP `/history`) | L | **Spec: `planning/picks-alpha-evaluation.md` Parts 1, 3.1, 4.** Blocked on real daily OHLC — stooq is unreachable from this env (anti-bot JS challenge). Unlock is the FMP key already in the Worker: add a `/history` endpoint or offline FMP pull, then compute gap-accurate stop simulation (entry = pick Price, stop = displayed risk level), true within-5-session stop-out rate, and **R-multiple expectancy** (the number the owner most wants — a 35%-win system is fine if avg-win/avg-loss > ~2). Until built, the internal price-chain numbers are survivorship-biased upper bounds. Do NOT start before PICKS-4a lands. |
| PICKS-4C | **Rename `risk_*_pct` → `risk_*_frac` (naming footgun)** | `scripts/picks_metrics.py`, `scripts/picks_config.py`, `docs/index.html`, `data/picks/picks*.csv`, `tests/` | S | **Spec: `planning/picks-alpha-evaluation.md` Part 3.2.** `risk_20ma_pct`/`risk_50ma_pct` are stored as **fractions** (median 0.06 = 6%) despite the `_pct` suffix — intentional and PWA-correct, but it misled the analyst on the alpha assessment and anyone building stop logic off the raw column is one `/100` from 0.06%-wide stops. Superset migration via `ensure_picks_csv()`; keep the old column readable through one release. Low-risk, prevents a future correctness bug. |
| ~~PICKS-METH~~ | ~~**Picks methodology tracking (versioned display constants + replay CLI)**~~ | `data/picks/display_methodology.json`, `tests/test_picks_methodology.py`, `scripts/replay_picks.py`, `tests/test_replay_picks.py` | M | ✅ **Done 2026-07-01.** Per `planning/picks-methodology-tracking.md`: v1 entry versions the base filter, All-view sort, Focus DQ, 3-component Focus score, and ATR display bands; anti-drift guard checks every param against live `docs/index.html` constants (14 tests); `replay_picks.py` deterministically reconstructs a historical All/Focus view from `picks.csv` + the methodology JSON for A/B testing across versions (20 tests). Triple-documented (CLAUDE.md, README.md § Configurable parameters, in-code). See PICKS-METH-V2 below for the known gap this surfaced. |
| ~~PICKS-METH-V2~~ | ~~**Extend display_methodology.json to cover Phase 3d Focus haircuts**~~ | `data/picks/display_methodology.json`, `data/picks/ariel_match_config.json`, `scripts/replay_picks.py`, `tests/test_picks_methodology.py`, `tests/test_replay_picks.py` | M | ✅ **Done 2026-07-01 (same session as PICKS-METH — CTO caught the gap immediately).** `v2` entry (effective 2026-07-01, confirmed via merge-commit timestamps for PR #224/#226) adds the liquidity gate/penalty (`FOCUS_MIN_DOLLAR_VOL`/`LIQUIDITY_PENALTY_START/MAX`) and earnings-proximity penalty (`EARNINGS_IMMINENT_DAYS`/`CAUTION_DAYS`/`PENALTY_MAX`/`POST_EARNINGS_PENALTY_FRAC`) on top of v1. `replay_picks.py`'s `_replay_focus()` is now version-shape-aware (v1 flat `focus_dq` vs v2 nested `{atr, liquidity}`) and applies both new multiplicative haircuts, including in the n=1 short-circuit path (matches JS `computeFocusScores` exactly). Anti-drift test now scoped to `current` only (v1 is a frozen snapshot, expected to diverge). Ariel-match (CTO call) got its **own** file, `ariel_match_config.json`, with **no anti-drift test** — it's an optional additive layer, not part of core All/Focus ranking; enforcement is manual review only. Permanent (not closeable) limitation: the earnings penalty is relative to viewer wall-clock `now` in the live PWA, so replay uses `--date` as a stand-in reference — documented in the JSON and module docstring, not treated as a bug. 61 tests total (up from 34) across both methodology test files. |
| ~~DOC-DRIFT-1~~ | ~~**Fix stale ATR constants in README § PWA display thresholds**~~ | `README.md` | XS | ✅ **Done 2026-07-01.** `ATR_EXT_ACTIONABLE` `5.0`→`4.0`, `ATR_EXT_TRIM` amber-band text `5–8×`→`4–8×`, `ATR_EXT_PENALTY_START` `3.5`→`2.5`, matching `CLAUDE.md` and live `docs/index.html`. Re-diffed the entire PWA-display-thresholds table against `docs/index.html` constant-by-constant (script-checked) to confirm no other value had drifted — none had. |

---

#### Ticker Lookup Feature

Full plan: `planning/PLAN_ticker_lookup.md`

| # | Task | File(s) | Effort | Notes |
|---|------|---------|--------|-------|
| ~~TICKER-0~~ | ~~**Taxonomy map: FMP → Finviz (Claude session)**~~ | `data/taxonomy_map.csv`, `data/fmp_sample_profiles.json`, `knowledge/fmp-api-findings.md` | S | **Done 2026-06-14.** Sampled 242 live FMP profiles → 129 unique FMP industries; built 133-row map, all Finviz names validated, 132/144 Finviz industries reachable (FMP coarser in 12 — documented). 12 low-confidence rows flagged. Evidence + API findings committed. |
| ~~TICKER-1~~ | ~~**CF Worker: /lookup endpoint + KV cache**~~ | `worker/src/index.js`, `worker/wrangler.toml`, `worker/src/taxonomy_map.json`, `worker/package.json`, `worker/README.md`, `worker/test/index.test.js` | M | ✅ **Done — deployed live 2026-06-14:** `https://finviz-ticker-lookup.salmonbaby8.workers.dev`. Code merged PR #74; deployed headlessly via `CLOUDFLARE_API_TOKEN` from a Claude Code web session (KV namespace `3ae4430b…`, FMP key set as Worker secret). 28 vitest tests pass; `/health`, `/lookup?t=AAPL` (→ Technology / Consumer Electronics, conf 1.0, KV cache verified), `/lookup?t=FAKEXYZ` (→ ticker_not_found) all pass live. Headless deploy writeup: `knowledge/cloudflare-headless-deploy.md`. |
| ~~TICKER-2~~ | ~~**PWA Lookup tab**~~ | `docs/index.html` | M | ✅ **Done 2026-06-14.** New "Lookup" tab wired to live Worker (`WORKER_URL`). Ticker input → `/lookup` → company header (logo/exchange/mktcap/confidence) + industry perf card + sector perf card + FAVORABLE/MIXED/CAUTION signal. Joins to already-loaded `state.data.{sectors,industries}.{delta,snap}` by exact Finviz group name (verified match). sessionStorage cache; graceful error cards. |
| ~~TICKER-3~~ | ~~**Streamlit Lookup tab**~~ | `dashboard/app.py`, NEW `dashboard/worker_client.py`, `requirements.txt`, `requirements-test.txt`, NEW `tests/test_worker_client.py` | M | ✅ **Done 2026-06-14.** Tab 8 "Ticker Lookup": calls live Worker via pure `lookup_ticker()` (no st import → testable), renders company header + Finviz classification + industry/sector `_render_group_card` (rank/momentum/perf joined to latest CSV date). `WORKER_URL` from `st.secrets`/env with live default. `requests==2.33.1` pinned. 4 new tests in `test_worker_client.py` (168 passed total, ex-playwright). |
| ~~TICKER-4~~ | ~~**Operations setup**~~ | — | — | ✅ **Done 2026-06-15.** Added FMP call counter (daily KV key `fmp_calls_YYYY-MM-DD`, 7d TTL), `/stats` endpoint returning `{date, fmp_calls_today}`, `DELETE /cache?t=TICKER` for manual cache busting. Counter incremented only on FMP cache misses, never on errors. 34 vitest tests pass. PR #90 merged. |
| TICKER-5 | **[FUTURE] Sector/Industry → Stocks screener** | `worker/src/index.js` (add /stocks endpoint), `docs/index.html`, `dashboard/app.py` | M | New Worker endpoint `/stocks?finviz_sector=&finviz_industry=` calls FMP screener, returns top 25 by market cap, KV cache 7d. Both front-ends add "Show stocks" toggle on group cards. Do NOT start until TICKER-0 through TICKER-4 are validated in production. See Phase 7 in plan. |

| ~~ETF-1~~ | ~~**Curated ETF→Finviz-group override layer (Lookup)**~~ | — | — | ✅ **Done 2026-06-20.** 31 curated ETF overrides (15 thematic/11 sector SPDRs/5 diversified). Build validation against snapshot CSVs. Runtime `lookupEtf()` + `fetchProfile()` wiring. PWA renders ETF kind badges + diversified informational card. ADR-005, worker/README, CLAUDE.md updated. 50 worker tests + 165 Python tests pass. Phase 2 (Finnhub holdings) deferred, design note in ADR-005. |
| ~~RS-3~~ | ~~**Phase 3: Surface RS signals in PWA**~~ | `docs/index.html`, `docs/sw.js`, `docs/releases.json`, `knowledge/moaty-metrics.md`, `tests/test_guide_releases.py`, `README.md`, `CLAUDE.md` | M | ✅ **Done 2026-06-21.** New "vs Mkt" tab (RS Score leaderboard + Emerging/Fading sub-views), Today card rs_month chip + rs_slope glyph, RS_STRONG/RS_SLIGHT constants, GUIDE 6 new entries, release triplet (v19). PR #139 open. Tier 5 discrete flags deferred → RS-4. |
| ~~RS-4~~ | ~~**Tier 5 RS discrete flags**~~ | `scripts/delta_config.py`, `scripts/compute_deltas.py`, `docs/index.html` | L | ✅ **Done 2026-06-21.** `beats_benchmark_{day..ytd}` (0/1), `rs_new_high` (NH badge, 20-session high), `rs_cross` (↑ cross badge, crossed 0 in last 5 sessions). 9 new schema columns auto-migrate. NH + ↑ cross badges on vs-Market RS Score + Regime cards; "beats N/7 tf" sub-line. 3 new GUIDE entries + moaty-metrics.md + README + CLAUDE.md + release triplet (v21). 206 tests pass. |
| RS-4b | **[OPT] Suppress neutral rsChip on Today cards** | `docs/index.html` rsChip() | S | Add `Math.abs(v) < RS_SLIGHT` guard to suppress chip inside ±0.5pp neutral band. Evaluate after SPY data has been live a few weeks to see if grey chips read as useful "data confirmed" signal or as noise. Current behavior (always show) is intentional — see code comment in rsChip(). |
| SW-UPDATE-UX | **[OPT] Soften SW update to toast instead of auto-reload** | `docs/sw.js`, `docs/index.html` | S | Currently the SW broadcasts `SW_RELOAD` on activate → tab auto-reloads silently. If this proves disruptive (e.g. user mid-session loses scroll/state), switch to Option 2: post `{ type: 'SW_UPDATED' }` and show a "New version — tap to refresh" toast instead. The TODO comment in `sw.js` activate handler marks the exact spot. Only do this if Option 1 causes real complaints. |

> **Phase 0:** (1) FMP free account + API key ✅ done. (2) Cloudflare account + KV namespace ✅ done — Worker deployed 2026-06-14.

> **Monthly recurring:** CF analytics check, FMP quota check, taxonomy validity spot-check. See plan Phase 5.

---

#### Lookup Tab Improvements

Full plan: `planning/lookup-tab-improvements.md`. ADRs: `knowledge/decisions/ADR-001..003`. Metric inventory: `knowledge/moaty-metrics.md`. Develop on `claude/lookup-tab-improvements-h7nw9b`. All Phase 1 slices are client-side in `docs/index.html` (no pipeline change, HTML-only — note in commits).

| # | Task | File(s) | Effort | Notes |
|---|------|---------|--------|-------|
| ~~LOOK-0~~ | ~~**Phase 0: knowledge + plan + README moat**~~ | `planning/lookup-tab-improvements.md`, `knowledge/moaty-metrics.md`, `knowledge/decisions/ADR-001..003`, `knowledge/cloudflare-edge-roadmap.md`, `README.md`, `.session/SPRINT.md` | S | Pickup-able plan, metric inventory, 3 ADRs, CF roadmap, README "What makes this different" section. No behavior change. |
| ~~LOOK-1~~ | ~~**Slice 1: retain history + weekly-rank sparkline**~~ | `docs/index.html`, `docs/sw.js` | M | ✅ **Done 2026-06-16.** `loadGroup` now retains full delta history in `state.data[group].deltaAll` (latest-only `.snap`/`.delta` untouched). New `groupRankHistory()` + `rankSparkline()` render an inline SVG of `rank_week` over the last ~30d in each group card, y inverted (up = improving), green/red by net direction, labeled "Weekly rank · last Nd". Hidden when <2 points. SW cache → v4. |
| ~~LOOK-2~~ | ~~**Slice 2: conviction chip + Rank Floor**~~ | `docs/index.html` | M | ✅ **Done 2026-06-16.** New `convictionInfo(delta, n)`: Rank Floor = max(rank_month, rank_quarter, rank_half) → "Top #{floor} across 1/3/6mo" row. Chip = "Sustained" (floor ≤ top quartile, emerald) / "Consistent" (rank_agreement ≥ 0.85 AND floor ≤ top half, sky) / hidden. Returns null gracefully if the 3 ranks aren't all present. Chip shown top-right of each group card. |
| ~~LOOK-3~~ | ~~**Slice 3: breadth dot strip**~~ | `docs/index.html` | S | ✅ **Done 2026-06-16.** `breadthStrip(snap)` renders D·W·M·Q·6M·Y dots (green/red/grey per `perf_*` sign) + an "All green" badge or "k/4 green" count. Verdict gates on month/quarter/half/ytd only via `BREADTH_TFS[].gate`; day & week dots render but don't gate (ADR-003). |
| ~~LOOK-4~~ | ~~**Slice 4: evidence-backed SIGNAL copy**~~ | `docs/index.html` | S | ✅ **Done 2026-06-16.** New `groupReasons(name, gd, n)` extracts concrete signals (30d/7d rank trajectory, conviction+floor, momentum %, breadth k/4). `contextSignalCard` appends the 2–3 strongest (industry first) as an evidence line under the verdict. Scoring spine + thresholds unchanged. |
| ~~LOOK-5~~ | ~~**Slice 5: clarity wins**~~ | `docs/index.html` | S | ✅ **Done 2026-06-16.** Rank label now "Rank (wk)"; added a 30d rank-delta chip (`rank_week_delta_30d`, "▲N over 30d") beside the weekly arrow; replaced "Looking up…" text with `lookupSkeleton()` matching the result layout. |
| ~~LOOK-6~~ | ~~**Slice 6: QoL — glossary + info affordance + deeplinks**~~ | `docs/index.html` | M | ✅ **Done 2026-06-16.** `lookupGlossary()` = collapsed "Why this matters" `<details>` (copy from `knowledge/moaty-metrics.md`) covering rank/floor/sustained/momentum/breadth incl. the percentile basis (folds in the info affordance). Subtle Finviz (`quote.ashx?t=`) + **TradingView** (`/symbols/SYM/`) deeplinks in the company header. Deepvue dropped — no public per-ticker URL (behind login); owner chose TradingView. |
| ~~LOOK-S1~~ | ~~**Lookup group search + typeahead (Ideas 1+2+4)**~~ | `docs/index.html` | M | ✅ **Done 2026-06-20.** Local group name search (case-insensitive, exact match, industries preferred over sectors). Typeahead dropdown ≥2 chars, prefix-first, 6 results, highlighted match, S/I badges, 40px touch, ↑↓ Enter Esc Tab keyboard nav. `doLookup()` refactored: group match → local; no match → Worker ticker path unchanged. Placeholder updated to "Search ticker or group…". `planning/lookup-search-enhancements.md` Ideas 1+2+4. |
| ~~LOOK-S2~~ | ~~**Aggregated group view (Idea 3)**~~ | `docs/index.html` | M | ✅ **Done 2026-06-20.** `groupPerfCard` gains `expanded = true` mode: all-7-timeframe rank+perf grids, YTD rank delta table for all windows, momentum deep dive (confirmed, accel, regime, rank trend). Sparse fields show `—`. Applied to both group-name path and ticker path cards. `lookupGlossary` extended with 5 new metric explanations. GUIDE tabs updated for lookup context. |
| LOOK-B1 | Sparkline rank-timeframe toggle (wk/mo/3mo/6mo) | `docs/index.html` | S | Deferred (Proposal A). |
| LOOK-B2 | Acceleration hint from `perf_*_delta_*` (▲▲/▼) | `docs/index.html` | S | Deferred (Proposal B7). |
| ~~LOOK-B3~~ | ~~Recent searches + pinned favorites (Idea 5)~~ | `docs/index.html` | S | ✅ **Done 2026-06-20.** `localStorage` keys `fvg_lookup_recent` (max 8, FIFO, pinned exempt) + `fvg_lookup_pinned` (no cap). Chips below search bar when empty; hide on typing/result. Pin ☆/★ button on every result card. Graceful degradation on private browsing / storage full. |
| ~~LOOK-B4~~ | ~~Fuzzy / "did you mean" matching (Idea 6)~~ | `docs/index.html` | M | ✅ **Done 2026-06-20.** `GROUP_SYNONYMS` constant maps colloquial names (semis, pharma, banks, biotech, …). Levenshtein fuzzy match with ratio threshold 0.35 catches typos ≥5-char queries; short ticker-like inputs (≤4 chars, no space) skip fuzzy. "Did you mean X?" prompt requires user confirmation. |
| ~~LOOK-B5~~ | ~~Empty-state suggestion chips (Idea 7)~~ | `docs/index.html` | S | ✅ **Done 2026-06-20.** `topMomentumChips()` returns top 5 groups by `momentum_score` from in-memory state. Shown when input is empty and no saved pinned/recents exist; hidden otherwise. Zero network calls. |
| LOOK-B6 | Tap group card → jump to group in Today/Momentum | `docs/index.html` | M | Deferred (Proposal D10). |
| ~~LOOK-SIG2~~ | ~~Signal card v2 — factor-based composite, RS folded in, "This stock" block~~ | `docs/index.html`, `tests/test_pwa_lookup_signal.py` (NEW), `.github/workflows/tests.yml`, `CLAUDE.md`, `README.md`, `knowledge/moaty-metrics.md`, `planning/lookup-tab-improvements.md` | M | ✅ **Done 2026-07-04.** `groupScore()` → `groupSignal()`: weighted composite over `momentum_confirmed`/`rs_confirmed`/rank delta/`regime_short_long`/breadth, missing factors excluded+renormalized (no more fake-neutral 0.5), evidence text generated from the same factor list that produced the score. RS surfaced on group cards (`rsChip`/`rsBeatsChip`). `lookupGlossary()` now generated from `GUIDE.metrics` (fixes the rs_score/rs_confirmed 'lookup'-tab drift). New "This stock" block (`findTickerPickInfo()`) surfaces the searched ticker's own Stage-2 category/ATR-extension/Focus score when it's in today's picks. Copy reworked off long-only phrasing. First test coverage for the Signal card (8 Playwright tests). See `planning/lookup-tab-improvements.md` § Phase 2. |
| LOOK-B5 | AI rotation-phase line on sector card | `docs/index.html` | S | Deferred. |
| LOOK-B6 | Promote Rank Floor to `compute_deltas.py` column (+ dashboard + tests) | `scripts/compute_deltas.py`, `dashboard/app.py`, `tests/` | M | Deferred. Product-wide consistency. |
| LOOK-B7 | Revisit All-Green week gating; align dashboard | `docs/index.html`, `dashboard/app.py` | S | Deferred. See ADR-003. |

---

#### AI Integration

| # | Task | File(s) | Effort | Notes |
|---|------|---------|--------|-------|
| AI-MIGRATION | **Migrate from Gemini AI Studio to Vertex AI (Phases 1–4)** | `scripts/generate_ai.py`, `.github/workflows/generate_ai.yml`, `tests/test_generate_ai.py`, `CLAUDE.md` | L | Plan in `planning/vertex-ai-migration.md` (PR #67). **Phases 2–4 merged in PR #79** (2026-06-14): dual-mode client, WIF workflow, PR #83+#84 fixes. **Phase 1 (GCP infra) = owner, done** — 3 secrets added; script confirmed on Vertex AI backend. **⚠️ BLOCKER: `sectors.daily_delta` field fails JSON parse** ("Unterminated string" error, column 99). PR #84's 900-token increase insufficient; response appears truncated or wrapped. 6/7 fields generating cleanly. **Next:** Debug actual response format from Vertex AI, increase tokens further or reduce daily_delta scope. |
| PLAN-2 | **Phase 2: Schema Enrichment + Few-Shot** | `scripts/generate_ai.py`, `tests/test_generate_ai.py` | M | Add `description` fields + `additionalProperties: false` to all 5 schemas; fix `_normalize_phase()` confidence bug; add few-shot examples to briefing/watchlist prompts; add validation logging. **BLOCKED** until Phase 1 is deployed and 2+ weeks of `fetch_log.csv` data shows skip logic firing correctly (`ai_outcome=skipped` on no-data days, `=complete` on data days). |
| AI-1 | **Anomaly Detection + LLM Explanation** | `scripts/generate_ai.py`, `dashboard/app.py` | M | Flag rank deltas >2σ from a 14-day rolling window using pandas, then send each flagged group to Gemini for a 1-sentence contextual note. See full spec below. |
| AI-2 | **Natural Language Q&A** | `dashboard/app.py` | M | Text input in AI Insights tab — user types a question, gets a plain-English answer backed by the actual data. Requires a real-time API call; needs an auth/cost-gate decision. See full spec below. |
| ~~AI-3~~ | ~~**Restore per-field resumability in `generate_ai.py`**~~ | — | — | **Done in PR #58** (2026-06-13). Restored incremental partial-file loading. Also fixed daily quota abort (`DailyQuotaExhaustedError`) and delta error tracking. |
| AI-4 | **AI Health widget in Streamlit dashboard** | `dashboard/app.py` | S | PR #53 decoupled AI generation, so `fetch_log.csv` no longer shows AI outcomes. Add a health widget to the AI Insights tab reading from `data/ai/index.json`. No pipeline changes needed. See full spec below. |

**AI-1 spec — Anomaly Detection + LLM Explanation**

_What it does:_ Nightly, detect statistically unusual rank moves and add plain-English context for each one.

_Implementation (all in `scripts/generate_ai.py`):_
1. Add `detect_anomalies(delta_df: pd.DataFrame, min_days: int = 14) -> list` — loads ALL rows of `deltas.csv` (not just latest day), computes per-group rolling 14-day mean and std of `rank_ytd_delta_7d`, then for the latest date flags any group where `|rank_ytd_delta_7d - rolling_mean| / rolling_std > 2`. Returns list of `{"name": str, "delta": float, "z_score": float}`. Return `[]` if fewer than `min_days` rows exist — don't guess.
2. In `generate_for_group()` (after watchlist), if anomalies detected, build a prompt: _"The following groups had unusually large rank moves today (vs. their 14-day baseline). For each, write one sentence explaining what this kind of move might indicate about capital rotation. [anomaly list]. Respond as NAME: [note] one per line."_ Catch exceptions same as the other 3 calls.
3. Parse the response into `result["anomalies"] = [{"name": ..., "delta": ..., "z_score": ..., "note": ...}]`.
4. JSON output already committed nightly alongside other AI content.
5. Dashboard: in `dashboard/app.py` tab 7 (AI Insights, end of file), add a `st.expander("Notable Moves")` after the briefing section. Iterate `ai_data.get(group_key, {}).get("anomalies", [])` and render each with delta + z_score + note. Show "None detected" if list is empty.

_Test additions (`tests/test_generate_ai.py`):_ `test_detect_anomalies_returns_empty_below_min_days`, `test_detect_anomalies_flags_high_z_score`, `test_detect_anomalies_no_false_positive_normal_move`.

_Data gate:_ needs 14+ days of delta history. Gate with `if len(all_dates) < min_days: return []`.

---

**AI-2 spec — Natural Language Q&A**

_What it does:_ A text input in the AI Insights tab — user asks "which industries have improved rank for 30 days straight?" or "show me everything with high momentum but weak recent move" and gets a direct answer.

_Architecture decision required (pick one before starting):_
- **Option A (recommended):** Gate on `GEMINI_API_KEY` in `st.secrets` — if present, show the Q&A widget; if absent, show a muted info message. Key stored in `.streamlit/secrets.toml` (gitignored) locally, or in Streamlit Cloud's secrets UI for deployment. This means the API key lives in the Streamlit environment — acceptable if the dashboard URL is not widely shared.
- **Option B (local-only):** Only enable Q&A when running locally (`os.getenv("GEMINI_API_KEY")` set). No key in Streamlit deployment; hosted dashboard stays key-free. Good for personal use only.

_Implementation (`dashboard/app.py`, tab 7, bottom of the file):_
1. Check `api_key = st.secrets.get("GEMINI_API_KEY", None) or os.getenv("GEMINI_API_KEY")`. If None, show `st.info("Configure GEMINI_API_KEY to enable Q&A.")` and stop.
2. `question = st.text_input("Ask a question about the data...")` + `st.button("Ask")`.
3. On submit: serialize the latest `delta_df` as a compact markdown table (top 30 rows by momentum_score, columns: name / rank_ytd / rank_ytd_delta_7d / momentum_score / rank_agreement). Pass with question to `gemini-1.5-flash`. Cache with `@st.cache_data(ttl=300, show_spinner=False)` keyed on `(question, str(latest_date), group_label)`.
4. Render response with `st.markdown(response)`.

_Token budget:_ 30 rows × 5 columns ≈ ~500 tokens of data context. Well within Flash's 1M limit. Log `len(prompt)` on first run to confirm.

_Test:_ mock `genai.GenerativeModel.generate_content`, verify prompt contains the question and data table, verify cache key includes the date.

---

**AI-3 spec — Restore per-field resumability in `generate_ai.py`**

_Background:_ PR #35 (2026-06-11) implemented field-level resumability — load the existing partial JSON for today, regenerate only missing fields. PR #42 (2026-06-12) removed it entirely: the "if today's file exists, skip everything" idempotency check was causing stale insights on days when Finviz updated after the initial run. The fix in PR #42 swung too far — it removed resumability as collateral damage. PR #50 (2026-06-13) added a run-level skip gate (`_has_new_delta_data`). As of now:

- `existing_output = {}` is hardcoded in `main()` — never populated from file
- The per-field skip logic in `generate_for_group` (`if spec["name"] in result: continue`) is orphaned dead code that never fires
- Every run makes all 7 API calls from scratch, even if 4 already succeeded before a rate-limit failure

_What to implement:_ Before starting generation, if `output_path` (`data/ai/YYYY-MM-DD.json`) already exists for today, load it into `existing_output` and set `was_incremental = True`. If the file is already complete (per `_is_complete()`), skip the whole run — unless `--force-ai` is set. The existing per-field skip logic in `generate_for_group` then fires correctly for any already-present fields.

_Critical constraint — don't restore the PR #42 stale data bug:_ The PR #42 bug was: file existed from an early cron run → second cron run (after Finviz updated) saw the file and skipped everything → stale data persisted all day. The fix: only load the existing file if it is **incomplete** (partial). A complete file skips the run (no stale risk since all fields are done). A partial file resumes only missing fields (correct). `--force-ai` always regenerates everything regardless.

_Decision table:_
| State | Behavior |
|-------|----------|
| No file for today | Generate all 7 fields |
| Partial file for today | Load it; generate only missing fields |
| Complete file for today | Skip (log `outcome=skipped`) |
| Any state + `--force-ai` | Generate all 7 fields from scratch |

_Files:_
- `scripts/generate_ai.py`: modify `main()` — add file-load block before the generation loop (8–10 lines)
- `tests/test_generate_ai.py`: add 2 tests — `test_main_resumes_partial_file` (partial file → only missing fields called), `test_main_skips_complete_file` (complete file, no force → zero API calls)

_Effort:_ S — the scaffolding is already there. The only missing piece is the 8-line block that reads the file into `existing_output`.

---

**AI-4 spec — AI Health widget in Streamlit dashboard**

_Motivation:_ PR #53 decoupled AI generation from `collect.yml` — `ai_outcome` in `fetch_log.csv` is now always `""` for snapshot rows. There is no single place to see "did AI run today, and did it succeed?" without digging into Actions logs or raw JSONL. This adds a lightweight visibility widget to the dashboard.

_Data source:_ `data/ai/index.json` — already committed nightly by `_update_index()` in `generate_ai.py`. Structure: `{"updated_at": "...", "entries": [{"date": "YYYY-MM-DD", "status": "complete|partial|skipped|failed", "model": "...", "generated_at": "...", "rotation_phase": "..."}, ...]}`. Capped at 90 entries. File is ~5KB — fast to read.

_Implementation (`dashboard/app.py`, AI Insights tab):_
1. After the existing AI content renders, add `st.expander("AI Run Health", expanded=False)`.
2. Inside: `index_path = DATA_DIR / "ai" / "index.json"`. If it doesn't exist, show `st.info("No AI run history yet.")`.
3. Load the JSON, take the first 7–10 entries (last 7–10 days). Render as a table or `st.metric` row:
   - Date | Status | Model | Generated at | Phase
   - Color-code `status` with emoji: `complete` → ✓, `partial` → ~, `skipped` → ○, `failed` → ✗ (or use `st.success/warning/info/error` per row)
4. For `partial` entries: the full field-level detail is in `data/ai_run_log.jsonl`. Optionally add a nested expander "Show field detail" that reads that entry from the JSONL (match on `date`). This is a nice-to-have — the outer widget is the priority.

_No test required_ — dashboard-only change. Note it in the commit message.

_Effort:_ S — `index.json` is already being written; this is a pure read.

_Dependency:_ None. Works today.

---

#### Start Here Onboarding — deferred items

| # | Task | File(s) | Effort | Notes |
|---|------|---------|--------|-------|
| PWATEST-LOOKBACK | **Fix 3 pre-existing `TestPWALookbackWindows` failures** | `tests/test_functional_playwright.py` | S | These failures pre-date the Start Here feature and are unrelated to it. Tracked here so they don't get waved off permanently. Likely covered by PWA-TEST-GAP work — resolve together. Do not block Start Here commits on this. |
| ONBOARD-DL-UX | **Revisit carousel deep-link dismiss behavior** | `docs/index.html` | S | Current decision (two-way door): "Open →" on a mid-tour slide calls `switchTab()` + dismisses the carousel immediately (sets `fvt_intro_seen_v1`). Rationale: user chose to navigate; leaving carousel open behind would be confusing; they can re-open via hub "Start Here". Revisit if user feedback shows they want to browse back mid-tour without losing their slide position. Code is marked with `// ONBOARD-DL-UX` comment. |

---

#### Lookback config + momentum variants

Plan: `planning/compute-deltas-lookbacks-and-momentum.md`. Slices 1–5 landed on
`claude/jolly-darwin-fjik54` (config-driven wide schema, trading-day 5/10/20/50
lookbacks, six momentum variants, PWA minimal renumber, generate_ai repointed).

| # | Task | File(s) | Effort | Notes |
|---|------|---------|--------|-------|
| ~~LB-FF1~~ | ~~**[FAST-FOLLOW] PWA full-dynamic lookback windows**~~ | `docs/index.html` | M | ✅ **Done 2026-06-18 (PR #110)**. Buttons now derived from CSV header via `extractWindowsFromHeader()`. Zero literal window values remain in JS. |
| PWA-TEST-GAP | **[FAST-FOLLOW] Fill PWA + Streamlit functional test gaps (PR #105 follow-up)** | `tests/test_functional_playwright.py` | L | The current Playwright tests only guard the Movers tab lookback buttons (PR #105 regression). Ten behavioral gaps remain untested. See the TODO(PWA-TEST-GAP) comment block at the top of `tests/test_functional_playwright.py` for the full gap list with detailed pick-up notes per gap. **Priority order (highest first):** Gap 7 (empty state), Gap 2 (Today tab), Gap 1 (Movers cards data), Gap 3 (Momentum tab), Gap 6 (Lookup tab + Worker intercept), Gaps 4/5/8/9/10. All tests use the same fixture-intercept pattern already in the file. Run with `playwright install chromium` then `pytest tests/test_functional_playwright.py -v`. |

#### PWA Column Gaps (PR#105 new columns not yet surfaced)

Full plan: `planning/compute-deltas-lookbacks-and-momentum.md` (see gap map). Surfaced in PWA as of 2026-06-18:

| # | Column | Status | Notes |
|---|--------|--------|-------|
| ~~MOT-R1~~ | `regime_short_long` | ✅ Done 2026-06-18 | Rotation view in Momentum tab |
| ~~MOT-A2~~ | `momentum_accel` | ✅ Done 2026-06-18 | ▲▲/▲/▼/▼▼ badge on Momentum cards |
| ~~TOD-S3~~ | `rank_trend_slope` | ✅ Done 2026-06-18 | Slope glyph ↑↑/↑/~/↓/↓↓ beside trend arrow on Today cards |
| ~~STR-C4~~ | `momentum_confirmed` | ✅ Done 2026-06-18 | Sort key in Strength tab (replaces momentum_score) |
| ~~TOD-P5~~ | `perf_week_delta_20d`, `perf_ytd_delta_20d` | ✅ Done 2026-06-18 | "vs 20d ago" row in Today expanded card |
| MOT-W1 | `momentum_weighted_mid` | Deferred | Similar concept to momentum_score; low marginal UX until users internalize existing view |
| MOT-W2 | `momentum_weighted_fast` | Deferred | Same rationale as MOT-W1 |
| MOT-M3 | `rank_month_delta_5d/10d/20d/50d` | Deferred | Could surface in Movers view alongside rank_ytd/rank_week deltas |
| GAP-1 | `rank_day`, `rank_year` | Deferred | Low priority; no obvious placement without cluttering existing cards |
| GAP-2 | `perf_month_delta_5d/10d/20d/50d`, `perf_ytd_delta_5d/10d/50d` | Deferred | 20d window surfaced (TOD-P5); other windows deferred |

**Data availability note:** `momentum_accel` and `rank_trend_slope` need 10 sessions (~2026-06-23). `perf_*_delta_20d` needs 20 sessions (~2026-07-10). These columns appear empty until then — their UI elements are hidden gracefully until data arrives.

#### Signal-to-Noise Monitoring

| # | Task | File(s) | Effort | Notes |
|---|------|---------|--------|-------|
| ~~SIG-NOISE-1~~ | ~~**`scripts/signal_noise.py` — rolling churn reporter**~~ | `scripts/signal_noise.py`, `tests/test_signal_noise.py` | S | ✅ **Done 2026-06-23.** Computes `mean \|day-over-day Δmomentum_score\|` over a rolling window (default 20 sessions) separately for sectors and industries. Baseline (8 days, 6-tf formula): sectors 0.058 / industries 0.046 (~40% better than old 7-tf formula). Run: `python scripts/signal_noise.py`. 11 tests. |
| SIG-NOISE-2 | **[FUTURE] Wire churn as CI assertion or dashboard panel** | `.github/workflows/tests.yml` or `dashboard/app.py` | S | Gate on ≥20 sessions of data. Proposed threshold: `mean \|Δmomentum_score\| > 0.07` = regression flag for both sectors and industries. Prevents a future formula change from silently re-introducing noise. Implement once session count crosses 20 (~2026-07-16 at 1 run/day). |

---

#### Data / Insight Features

| # | Task | File(s) | Effort | Notes |
|---|------|---------|--------|-------|
| D1 | **[USER ACTION] Create `main` branch, set as default** | GitHub UI | S | Blocks cron and PWA-1. Merge `claude/elegant-babbage-hlxnfy` → new `main`, then Settings → Branches → change default. |
| PWA-1 | Fix hardcoded `BRANCH` constant in PWA | `docs/index.html` line ~117 | S | Change `'claude/elegant-babbage-hlxnfy'` to `'main'` (or whichever branch receives daily cron data). Depends on D1. |
| INS-4 | **Momentum Velocity (`momentum_score_delta_7d/14d`)** | `scripts/compute_deltas.py` | M | Track momentum_score change over time. "Rising Stars" = positive velocity + currently top-half. Needs 7+ days of data. |
| INS-5 | **Daily Brief card (PWA top-of-screen)** | `docs/index.html` | M | Single card: today's breakout, sustained leaders, what's rolling over. Eliminates tab-hopping on mobile. Needs 7+ days for interesting content. |
| INS-6 | **Momentum Score Heatmap (time × industry)** | `dashboard/app.py` | S | Companion to existing rank-delta heatmap — cells = `momentum_score` over time. Absolute picture of sustained leaders. Needs 7+ days. |
| ~~TAX-0~~ | ~~**Seed sector→industry taxonomy map**~~ | `data/finviz_sector_industry_map.{json,csv}`, `scripts/seed_taxonomy.py` | S | ✅ **Done 2026-06-24 (PR #171).** Plain-HTTP parse of fasiha/finviz-git-scraper. 11/11 sectors, 144/144 industries match (100%). PR #109 superseded. 13 tests pass. |
| ~~TASK-6B~~ | ~~**Streamlit sidebar sector filter**~~ | `dashboard/app.py` | S | ✅ **Done 2026-06-24 (PR #177).** Duplicate of the entry above under Data Pipeline — kept for cross-reference, see that entry for detail. |
| ~~INS-7~~ | ~~**Sector Breadth metric**~~ | `dashboard/app.py`, `docs/index.html` | M | ✅ **Streamlit done 2026-06-24 (PR #177); PWA done 2026-07-02 (PR #231).** PWA side landed as a *different* feature than originally scoped here — see HIR-A/HIR-B: a breadth table shipped to the PWA Strength tab (`122a4d1`), and the Today-tab sector-card breadth bar shipped via PR #231. |
| DEBT-1 | `evict_today_rows` concurrency race | `scripts/collect.py` | S | Two simultaneous `collect.py` processes could race on read-modify-write. Non-issue given single scheduled Action + ad-hoc manual runs. Fix would be a file lock (e.g. `fcntl.flock`). Table until concurrency is actually needed. |
| DEBT-2 | `evict_today_rows` I/O errors not caught | `scripts/collect.py` | S | Disk-full / permission errors bubble up as exceptions. Intentional — matches rest of codebase. Could add explicit error message if this causes confusion in prod logs. |
| DEBT-3 | `DISPATCH_REF` in `worker-cron/wrangler.toml` hardcodes non-main branch | `worker-cron/wrangler.toml` | S | Set to `"claude/elegant-babbage-hlxnfy"` during Phase 1 (the current default). **Blocked by D1** — once D1 (create `main`, set as default) is done, change `DISPATCH_REF` to `"main"` and redeploy the Worker. The TODO(D1) comment in `wrangler.toml` marks the spot. |

---

#### Sector → Industry Hierarchy Features

Full plan: `planning/PLAN_sector_industry_hierarchy.md` — 22 features across 5 tiers (Navigation, Signal, Retention, Bridge, Trust). Foundation (TAX-0) ✅ done. All features below are consumers of `data/finviz_sector_industry_map.json`. Workstream resuming 2026-07-02 after the Picks pause — **read the plan's new "⚠️ Current State" section at the top before touching HIR-A/B/F.**

| # | Task | File(s) | Effort | Notes |
|---|------|---------|--------|-------|
| HIR-A | **PWA drill-down navigation** | `docs/index.html` | M | ✅ **Done 2026-07-02 (PR #231, merged).** Rebase + reconciliation of draft PR #178 against the Strength-tab breadth table that shipped separately (`122a4d1`). |
| HIR-B | **PWA breadth bar on sector cards (Today tab)** | `docs/index.html` | S | ✅ **Done 2026-07-02 (PR #231, merged).** Count + mini-bar, per VP decision. Distinct from the Strength-tab breadth *table* (`122a4d1`) — both wanted, different tabs. Ported to the Lookup tab's sector card same day (see HIR-C row — shipped together). |
| HIR-F | **Rank within sector** | `docs/index.html` | S | ✅ **Done 2026-07-02 (PR #231, merged).** Shown in the drill-down only (VP decision: not on the collapsed card). |
| HIR-C | **Leaders & Laggards mini-list within sector** | `docs/index.html` | S | ✅ **Done 2026-07-02.** Leader (🏆) / laggard (🔻) callout row, derived from the same `sectorIndustryRows()` helper the drill-down uses. Ships on both the Today-tab drill-down and the Lookup-tab sector card (breadth bar + this callout + full industry list ported there in the same change, per VP request). |
| HIR-R | **Market-wide breadth gauge** | `docs/index.html` | S | **Not started — spec is broken as written, not VP-approved.** "68% of industries top-half" is self-contradictory: "top half" by rank is definitionally ~50% of the universe, it can't read 68%. Needs a real metric definition (e.g. % of industries with `perf_ytd > 0`, or % beating SPY via `rs_score`) before this is buildable. VP flagged 2026-07-02 that the whole Tier-2 backlog reads as unapproved/fluffy — treat every HIR-* row below this one as **not started and not committed to**, pending a VP pass over the plan doc. |
| HIR-D | **Divergence alerts / Rotation Radar tab** | `docs/index.html`, `dashboard/app.py` | M | Auto-surface sector vs. breadth disagreements: "sector green, only 2/12 industries participating." Highest-signal rotation pattern. VP decision: new tab vs. section before coding. |
| HIR-M | **Crowding / concentration warning** | `docs/index.html`, `dashboard/app.py` | S | Inverse of breadth: flag sector gain carried by 1 industry. Risk signal missing from every momentum tool. |
| HIR-I | **Since-last-look digest** | `docs/index.html` | M | Store last-viewed timestamp in PWA local storage. On open: "Since Tuesday: Energy breadth 4→9, Semis entered top 10." Zero backend. Highest-leverage retention feature. |
| HIR-K | **AI brief with sector context** | `scripts/generate_ai.py` | M | Add sector/breadth context to existing briefing prompt. Prompt-only update — no schema change. |
| HIR-H | **Rotation flow map** | `docs/index.html`, `dashboard/app.py` | L | Sankey/flow: capital leaving fading sectors → entering emerging ones. Signature feature. VP charting library decision required before coding. |
| HIR-E | **Breadth-confirmed momentum column** | `scripts/compute_deltas.py`, `tests/` | S | New `momentum_breadth_confirmed` in `deltas.csv`. Schema migration required. |
| HIR-TAX-TRIPWIRE | **Taxonomy staleness tripwire** | `scripts/collect.py` or `tests/` | S | Warn when live industry name missing from map. Prevents silent breadth denominator errors. Anytime task. |
| HIR-O | **[FUTURE] Sector→Industry→Stocks bridge** | `worker/src/index.js`, `docs/index.html` | M | Entry point for TICKER-5. Do NOT start until TICKER-4 validated + HIR-A built. |
| HIR-N | **[DEFERRED Q4+] Historical analog** | TBD | L | Match breadth fingerprint to past setups. Needs 6+ months data. Revisit ~Q4 2026. |

| AUD-1 | **Stale-branch sweep + delete-on-merge setting** | GitHub repo settings | S | 2026-07-10 audit: 142 unmerged remote branches; ≥3 carry stranded session-notes commits (`chore/session-handoff-2026-06-10`, `chore/session-notes-pr50-merged`, more). Recover stranded notes into archive, bulk-delete, enable "Automatically delete head branches". See `knowledge/dev-process-audit-2026-07-10.md`. |
| AUD-2 | **Tests for `backfill.py` + `export_db.py`** | `tests/` | S | Only two scripts/ files with zero test coverage. Minimum: smoke test that export_db output columns == `delta_columns()`. |
| AUD-3 | **Add ruff lint gate** | `pyproject.toml`, `.github/workflows/tests.yml` | S | Repo has zero automated lint/format enforcement; all style discipline is manual. Add ruff config + CI step (start permissive, tighten later). |
| ~~AUD-4~~ | ~~**generate_ai.yml concurrency + collect.yml push rebase**~~ | `.github/workflows/` | S | ✅ **Done 2026-07-19.** collect.yml's `git pull --rebase` landed earlier with PICKS-4 (PR #250); this session moved generate_ai.yml from its own `generate-ai` concurrency group into `finviz-data-commit` — all three data/ writers now serialize on one group. Trade-off documented in the workflow comment: GitHub keeps only the newest *pending* run per group, so an AI run superseded while queued is dropped — recoverable via `workflow_dispatch` (force_ai), unlike a raced data commit. |
| AUD-5 | **Document `COLLECTED_AT_CRON_UTC` in README + root CLAUDE.md** | `README.md`, `CLAUDE.md` | S | Violates the 3-place rule; also decide whether `scripts/CLAUDE.md` counts as "CLAUDE.md" for the rule and write that down. |
| LB-FF1-RESIDUAL | **Remove last hardcoded `_20d` literals** | `docs/index.html` (~1741) | S | LB-FF1 marked done but `perf_week_delta_20d`/`perf_ytd_delta_20d` literals remain; either derive or amend the Done note's "zero literals" claim. |

> **D1 note — the elegant-babbage debt**: `claude/elegant-babbage-hlxnfy` is currently the default branch (no `main` exists). GitHub Actions cron only fires on the default branch, and the PWA hardcodes this branch name. D1 is the root fix; PWA-1 is the code follow-up. Until D1 is done, the cron data will keep landing on `elegant-babbage` — so don't change the `BRANCH` constant before D1 is complete.

---

### 🟡 Ready

| # | Task | File(s) | Effort | Notes |
|---|------|---------|--------|-------|
| PLAN-1 | **Phase 1: Smart Regeneration + Force Flag** | `scripts/generate_ai.py`, `.github/workflows/collect.yml`, `README.md` | M | Add `_has_new_delta_data()` helper (Task 1.1), argparse + force flag + skip gate in `main()` (Task 1.2), workflow input param (Task 1.3). Full spec in `planning/PLAN_smart_regeneration_pydantic.md`. Start new session; generate_ai.py is large. |

---

### 🟢 In Progress

| # | Task | Branch | Notes |
|---|------|--------|-------|
| WS5-2 | **Held-tickers feed → `ticker_quotes`** (#297, #312) | `claude/ws5-phase2-held-feed-gez3ja` | Mechanical plumbing landed (not yet committed at time of writing): `scripts/collect_held.py` (reuses `collect_morning.fetch_ticker_quotes(block="held")`) + `tests/test_collect_held.py` + `.github/workflows/collect_held.yml` (D1-write-via-HTTP, no git commit step) + `worker-cron` `held` scheduled job (17:30 ET, ungated). Worker endpoints (`GET /held-tickers`, `POST /ingest/quotes`) landed separately on `worker-positions/**` (security-critical, lead-owned). Not yet exercised against live D1/positions — see `worker-positions/README.md` § Phase status. **2026-08-13 first manual `collect_held.yml` dispatch failed** (`GET /held-tickers failed: HTTP 403 Forbidden`) — root cause was NOT missing secrets (owner had set both `POSITIONS_WORKER_URL`/`POSITIONS_INGEST_TOKEN` correctly); Cloudflare's Bot Fight Mode on the `*.workers.dev` zone blocks the default `Python-urllib/x.y` User-Agent with error 1010 before the request reaches the Worker's own auth code (verified live: even `/health`, unauthenticated, 403s under that UA). Fixed by setting a non-generic `User-Agent` header in `_authed_request()`; regression test added. Re-run `collect_held.yml` to confirm end-to-end before considering go-live complete.

---

### ✅ Done

| # | Task | Date |
|---|------|------|
| EFFORTB-POWER3-5 | **Effort B (issue #379) B-5 — "Power of 3" MA-bunching chip + shown MA distances.** Owner-renamed from "Rule of Three" (2026-09-02); price/20MA/50MA only (10MA not scraped, 200 dropped as not this pattern's third line). New 6th `METRICS_COLS` col `power_of_3` in `picks_metrics` = single fact `spread(price, 20MA$, 50MA$) ≤ POWER_OF_3_ATR_MULT(2.0)×ATR` — the 2×ATR band is **owner-specified** (domain authority sets it → a binary chip is §4.0-legit, not an invented cutoff), computed next to `stage2` reusing its reconstructed MA prices. PWA: a "MA bunching" sub-block in the shared `volSetupSectionHtml` (A-2 seam) — a green "Power of 3" chip when bunched + the two SHOWN SMA % distances; reaches Picks + Morning + Watchlist + Ticket for free via the existing B-6 `setupRowForCard` cross-ref (orphans self-hide). New constant `POWER_OF_3_ATR_MULT` triple-documented; no PWA constant (pipeline is the single source of truth). Release `2026.09.02.1` (feature, tab picks) / sw v91→v92. **Next: B-4 (compose VCP-style proxy from B-2+B-3+52W) — last named spine composite.** | 2026-09-02 |
| EFFORTA-CARDSEAM-A2-B6 | **Effort A A-2 + B-6/A-3 (issues #378/#379) — compression "Volatility & setup" section on the Morning family (PR #390).** A-2: extracted the section (B-1 Vol W/M·RelVol·52W + B-2 range tightening + B-3 volume dry-up) from `renderPickRow` into one shared `volSetupSectionHtml(r)` in `docs/index.html` (pure refactor, Picks card byte-identical, returns `''` on a data-less row). B-6/A-3: new `setupRowForCard(ticker, freshRow)` = `ws4FindPicksRow` picks_latest cross-ref (B-2/B-3 sparkline cols) + fresh morning-store B-1 override; rendered in `morningCardBody` (all live statuses, after metric rows before ticket/CTA) + `watchCardHtml` (after body). Trade ticket inherits from the morning card (no duplicate render); Lookup Stage-2 already had it via `renderPickRow`. Graceful degrade for orphans. Owner-approved mock `planning/mocks/b6-morning-volatility-setup.html`. Tests: +2 morning PWA (+ a `pre_close_latest.csv` stub so `test_pwa_morning.py` runs in the cloud sandbox), +1 watch PWA; picks 9/9; guide/releases 5/5. Release `2026.09.02` (feature, tab morning) / sw v90→v91. No new configurable constant. **Next: B-5 (MA bunching / Rule-of-Three) or B-4 (VCP-style proxy).** | 2026-09-02 |
| EFFORTB-VOLDRYUP-3 | **Effort B (issue #379) B-3 — "Volume dry-up" on the expanded Picks card.** 4th `TRAILING_COLS` col `relvol_spark` (pipe-joined trailing `Rel Volume` series) via the same `picks_metrics.compute_trailing_setup` machinery as B-2 — reuses `SPARK_WINDOW`/`SPARK_MIN_BARS`, **no new constant, nothing thresholded** (doc §4.0: a SHOWN trend, the strongest/cheapest owner-named VCP sub-signal — volume drying up as range tightens is the coiling tell). PWA: "Volume dry-up" sub-block under the B-1 "Volatility & setup" section via the existing `volSpark()` helper; self-hides on thin history. Migration (`ensure_picks_csv`) backfilled 487/535 picks_latest rows. Chose B-3 over A-2→B-6 this session (spine fact + contained/ephemeral-safe + free to land before B-6 propagates the whole section). Release `2026.09.01`, sw v89→v90. Tests: 1 new `compute_trailing_setup` unit + extended the B-2 PWA test (3 polylines + "Volume dry-up"); 737 non-PW green, PWA green (chromium-1234). **Next: A-2** (extract shared card component) → **B-6** (propagate B-1/B-2/B-3 to Morning family). | 2026-09-01 |
| EFFORTB-RANGETIGHT-2 | **Effort B (issue #379) B-2 — "Range tightening" on the expanded Picks card.** Trailing-window compression signals: 3 new `TRAILING_COLS` in the picks pipeline (`picks_config.py` / `picks_metrics.compute_trailing_setup`) — `tight_range_7` (1 if today's raw High−Low is narrowest of the last `TIGHT_RANGE_WINDOW`=7 **available** bars — gappy per-name history, so honest "last 7 bars", NOT "NR7"; owner call 2026-08-31), `range_atr_spark` / `atr_spark` (pipe-joined last ≤`SPARK_WINDOW`=10 values, `SPARK_MIN_BARS`=3 to emit). Computed over a ticker's trailing available bars (dedups same-date multi-bucket rows), populated only on the max-date picks_latest slice (older rows ""); wired into `write_picks` + `ensure_picks_csv` backfill. PWA: "Range tightening" block extends the B-1 "Volatility & setup" section — honest tightest-range flag + `volSpark()` mini sparklines (range/ATR + ATR $). Per-name graceful degrade throughout (facts→flags / shown-values, doc §4.0/§3). 3 constants triple-documented (in-code + README + scripts/CLAUDE.md). Release `2026.08.31.1`, sw v88→v89. Tests: 4 `compute_trailing_setup` unit + 1 PWA (`test_range_tightening_shows_flag_and_sparklines`, atr_earnings file 9/9 green via revision-symlink harness); 734 non-PW suite green. **Next B slices (#379):** B-3 volume dry-up (RelVol trend window), B-5 Rule-of-Three; B-6 propagate to Morning/Lookup rides Effort A (A-1 morning-data decision still open). | 2026-08-31 |
| EFFORTB-VOLSETUP-1 | **Effort B (issue #379) first slice — "Volatility & setup" section on the expanded Picks card.** Compression-spine facts SHOWN as raw values, no invented thresholds (governing principle §4.0, `planning/compression-expansion-ideation.md`): **Vol W / Vol M** (both raw, e.g. `4.3% / 4.6%`), **Rel volume** (`0.87×`), **From 52W high** (signed %, e.g. `−8.0%`). The only derived read is a `contracting`/`expanding` tint = pure sign of (Vol W − Vol M), a fact (which is larger), not a cutoff. Pure PWA display — all four inputs already flow daily in `picks_latest.csv` (`Volatility W/M`, `Rel Volume`, `52W High`); **no pipeline change, no new column, no new configurable constant.** Establishes the named "Setup/Volatility" section that Effort A (#378) will propagate to the Morning-family cards. 2 new Playwright tests in `tests/test_pwa_picks_atr_earnings.py` (in CI `--ignore`), verified live via revision-symlink harness (8/8 file green); 731 non-PW suite green. Release `2026.08.31`, sw v87→v88. **Next Effort-B slices (still open, #379):** NR7 flag + range_atr sparkline (need per-name trailing history → derived pipeline columns, graceful-degrade per doc §3); Rule-of-Three MA-bunching confirmer; propagate this section to Lookup/Morning cards (rides Effort A). | 2026-08-31 |
| WATCH-KEBAB-FIX | Fix watchlist "⋯" kebab menu never opening (Remove/Renew/Edit level all unreachable) — PR #370's `watchKebabHtml` compared `state.watchMenu` (always a string, since the onclick attribute quotes it) against `entry.id` (a raw D1 `INTEGER`, i.e. a JS number) with strict `===`; type mismatch meant `isOpen` was always `false`. Fixed by stringifying both sides. Reported by the owner ("I see a kebab but nothing happens when I tap"). Root-caused and reproduced via a minimal Node harness plus a real headless-browser Playwright run (revert-and-rerun proved `test_remove_sends_patch_not_delete` fails on the old code, passes on the fix). `tests/test_pwa_watchlist.py`'s `WATCH_ENTRY`/`removed_entry` fixture ids changed from strings (`"w1"`) to real numeric ids (`1`/`3`) — the string fixture had been silently masking this exact bug since PR #370 shipped. Also fixed an unrelated pre-existing gap in that same test file's `_base_routes()` (missing `pre_close_latest.csv` stub caused an unstubbed real network fetch to hang forever in this sandbox, per the known Root-Cause-2 gotcha in `knowledge/investigations/playwright-cloud-session-testing.md`). Release `2026.08.27.1`, sw v86→v87. | 2026-08-27 |
| WS3-RECLAIM-PICKS | Failed-breakdown / undercut-and-reclaim on Morning **Picks** (closes the "picks opting into `reclaim`" deferral from WS5-8b brief §10). Picks now emit `reclaim` against an ordered `reclaim_refs` = prior swing low OR derived 50MA (`collect_morning._pick_reclaim_refs`; 50MA derived from picks_latest's %-distance `SMA50`, ~1 session stale — accepted). `pick_status`: new `matched_reclaim_ref` + `reclaim_refs` param; **`reclaim` reordered ABOVE `failed_breakout`, uniform for picks + watch** (owner decision 2026-08-27, ADR-013 amended). Two new store columns `reclaim_ref`/`reclaim_ref_value` (superset-additive). PWA: `reclaim` case in `morningCardBody` naming the level (50MA `~`-prefixed), `MORNING_STATUS_META.reclaim.actionable` false→true. Release `2026.08.27`, sw v85→v86. 92 targeted + 731 full-suite pytest green; PWA test added to `test_pwa_morning.py` (verified by CI — Playwright unrunnable in this sandbox, known gotcha). **Kept invalidated > reclaim** (owner did not touch it). Still deferred: multi-day reclaim. | 2026-08-27 |
| PICKS-SELECTOR-V2 | Selector dedup backfill (ADR-007 amendment, `SELECTOR_VERSION` v1→v2): a group already selected by a higher-priority bucket still gets tagged when it naturally ranks in a lower-priority bucket's top-N (attribution preserved) but no longer consumes one of that bucket's N slots — emerging/accel/rs_new_high backfill past rank N with the next new candidate (`add_bucket_with_backfill`). Verified on real `deltas.csv`: 6/29 and 7/1 both went from 16 to 20 unique groups scraped. Paired with `PAGE_CAP` 15→2 (40-name cap) — historical data showed only Biotechnology (~100 names/day) ever exceeded 40. | 2026-07-02 |
| CF-DEPLOY | Auto-deploy Cloudflare Workers on push: `.github/workflows/deploy-workers.yml` — two independent jobs (ticker-lookup + cron-dispatcher), test-gated, path-filtered, `workflow_dispatch` available | 2026-06-21 |
| ONBOARD | Start Here onboarding: WELCOME constant, 5-slide first-run carousel, hub "Start Here" section, fvt_intro_seen_v1, anti-drift tests, canonical copy in product-intro-copy.md, release cut | 2026-06-21 |
| ETF-1 | Curated ETF→Finviz override layer: 31 overrides (thematic/sector/diversified), build validation, runtime wiring, PWA ETF badges | 2026-06-20 |
| LB-FF1 | PWA full-dynamic lookback buttons derived from CSV header (`extractWindowsFromHeader`) (PR #110) | 2026-06-18 |
| MOT-R1/A2, TOD-S3/P5, STR-C4 | Surface PR#105 momentum metrics in PWA: Rotation view, accel badge, slope glyph, confirmed sort, perf-delta row | 2026-06-18 |
| LB-1..5 | Config-driven delta schema (`delta_config.py`), trading-day 5/10/20/50 lookbacks, momentum variants (confirmed, weighted-mid/fast, regime, accel ⊃ INS-4, rank-trend slope), PWA renumber, generate_ai repoint | 2026-06-17 |
| AI-PWA | AI tab improvements (Items 1–8): key signals, delta card, conviction tags, industries structure, relative timestamp, native share, phase history strip, historical date navigation | 2026-06-12 |
| AI-ARCH | AI architecture revamp: `TASK_SPECS`, `index.json` manifest, `gemini-2.5-flash`, incremental completion (PR #38) | 2026-06-11 |
| MON-1 | Workflow logging + monitoring: AI partial completion fix, `ai_run_log.jsonl`, `fetch_log.csv` AI columns, PWA pipeline diamond (PR #35) | 2026-06-11 |
| AI-0 | Server-side AI pipeline: daily briefing + rotation phase + watchlist (PR #25) | 2026-06-10 |
| INS-1 | Sustained Strength / "Evergreen" list (Streamlit + PWA) | 2026-06-10 |
| INS-2 | `rank_agreement` metric in deltas.csv | 2026-06-10 |
| INS-3 | All Green filter + emoji dot matrix | 2026-06-10 |
| PWA-2 | Add `<link rel="apple-touch-icon">` for iOS homescreen icon | 2026-06-10 |
| PWA-3 | Show error on active tab (not just Today) | 2026-06-10 |
| PWA-4 | Dead code cleanup: `fmtPct` forceSign + `moverCard` delta shadowing | 2026-06-10 |
| — | First live scrape: 11 sectors, 144 industries | 2026-06-09 |
| — | End-to-end pipeline verified (collect → deltas → dashboard) | 2026-06-09 |
| — | GitHub Actions cron wired (weekdays 22:00 UTC) | 2026-06-09 |
| — | Scraper fixes: CSS selector, domcontentloaded, TLS, perf_day | 2026-06-09 |
| 1 | Test infrastructure: 50 tests, all green (`pytest tests/ -v`) | 2026-06-09 |
| 2a | `rank_day` metric added to delta schema; existing CSVs auto-migrated | 2026-06-09 |
| 2b | Momentum score NaN fix: all-NaN columns excluded from mean | 2026-06-09 |
| 3a | Rank columns (rank_day/week/month/ytd) in Snapshot tab | 2026-06-09 |
| 3b | CSV export buttons on Snapshot, Top Movers, Momentum tables | 2026-06-09 |
| 3c | Multi-select Time Series (up to 3 groups, color-coded) | 2026-06-09 |
| 4a | `collect()` post-parse row-count guard (RuntimeError on 0 rows) | 2026-06-09 |
| 4b | Unknown Finviz column names logged as summary line to stderr | 2026-06-09 |
| 4c | `fetch_html()` runtime timing logged | 2026-06-09 |
| 5a | GitHub Actions job timeout: `timeout-minutes: 30` | 2026-06-09 |
| 5b | GitHub Actions post-collect row-count verification step | 2026-06-09 |
| 6a | Heatmap tab (RdYlGn; gated behind ≥7 day data guard) | 2026-06-09 |
| T7 | Test: `collect()` row-count guard — 56 tests, all green | 2026-06-09 |
| T8 | GitHub Actions CI workflow (`tests.yml`) — YAML correct; see note below | 2026-06-09 |
| T9 | Test: `ensure_deltas_csv` all 3 paths | 2026-06-09 |
| R1 | `.claude/rules/commit-discipline.md` — commit sizing, test requirements, handoff checklist | 2026-06-09 |
| M1 | Mobile iPhone PWA (`docs/`): Today / Movers / Momentum tabs; GitHub Pages; Add to Home Screen | 2026-06-09 |

---

## Effort Key
| Label | Time |
|-------|------|
| S | < 1h |
| M | 1–2h |
| L | 2–4h |

---

## Next Milestones

| Date | Event |
|------|-------|
| ~2026-06-16 | 7d deltas available — Heatmap + Top Movers light up |
| ~2026-06-23 | 14d deltas available |
| ~2026-07-09 | 30d deltas available — full picture |
| ~2026-09 (≈60 sessions) | **Re-run Picks alpha assessment** — first read across a possible regime change. Confirm/kill the `leaders`-bucket-drag and rotation-bucket-edge hypotheses. See `planning/picks-alpha-evaluation.md` Part 2. |
| ~2026-11 (≈100 sessions) | **Re-run Picks alpha assessment** — powered enough (~40+ distinct dates) to consider selector/Focus-gate tuning. Not before. |

After 7d data arrives: consider adding `rank_day_delta_7d` to the delta schema (same pattern as `rank_week_delta_7d`).

---

## Verification Checklist

- [x] `pytest tests/ -v` — 56 tests pass
- [x] `python scripts/compute_deltas.py` — migrates existing CSVs, `rank_day` in output
- [x] Dashboard: rank cols in Snapshot, download buttons, Time Series multiselect, Heatmap "need 7 days" message
- [x] GH Actions `collect.yml` — timeout + row-count step present
- [x] Push branch; draft PR #3 created
- [x] T7: `collect()` guard tests added (TestCollectRowCountGuard)
- [x] T8: `tests.yml` CI workflow YAML added and correct (runner allocation issue is account-level)
- [x] T9: `ensure_deltas_csv` path tests added (TestEnsureDeltasCsv)
