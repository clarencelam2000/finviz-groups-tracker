__pycache__/
*.pyc
.env
exports/
*.db
*.parquet
.playwright/
node_modules/
.ipynb_checkpoints/

# Claude session files — committed so they survive ephemeral cloud containers
# .claude/session-notes.md  (tracked)
# .claude/WORK_LOG.md       (tracked)
.claude/worktrees/

# Probe debug HTML dumps (CI artifact only, never committed)
data/picks/debug_html/

# Throwaway build specs / working files (per-session, never committed)
scratchpad/
